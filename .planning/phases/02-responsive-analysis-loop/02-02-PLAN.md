---
phase: 02-responsive-analysis-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceEditor.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/styles.css
autonomous: true
requirements:
  - WORK-03
  - WORK-04
  - PERF-02
must_haves:
  truths:
    - User can keep typing while analysis is queued or running, without losing or freezing the current draft.
    - User sees analysis refresh automatically shortly after pausing input and can still trigger an immediate refresh for the current draft.
    - User can tell whether the visible analysis is queued, running, current, or failed before trusting it.
  artifacts:
    - { path: src/App.tsx, provides: "App-level orchestration between draft state and the background analysis scheduler", contains: "createAnalysisScheduler" }
    - { path: src/features/workspace/components/WorkspaceEditor.tsx, provides: "Editor controls and live status copy for automatic and manual refresh", contains: "Refresh now" }
    - { path: src/features/workspace/components/WorkspaceSnapshot.tsx, provides: "Analysis panel that surfaces background refresh status and freshness", contains: "Background analysis" }
    - { path: src/App.test.tsx, provides: "Integration coverage for debounce-driven analysis refresh and manual override", contains: "vi.useFakeTimers" }
    - { path: src/styles.css, provides: "Responsive styling for queued, running, and refreshed analysis states", contains: ".analysis-status" }
  key_links:
    - { from: src/App.tsx, to: src/features/analysis/lib/createAnalysisScheduler.ts, via: "effect-driven background refresh orchestration", pattern: "createAnalysisScheduler" }
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceEditor.tsx, via: "draft and refresh props handoff", pattern: "WorkspaceEditor" }
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "snapshot and lifecycle status handoff", pattern: "WorkspaceSnapshot" }
---

<objective>
Turn the current manual snapshot flow into a responsive background analysis experience the user can trust.

Purpose: Deliver the first real-time loop for the product so writing stays smooth, analysis refreshes on its own after edits, and the UI clearly communicates when the shown results are current.
Output: App wiring for debounced background analysis, updated workspace/status UI, and integration tests that prove the loop works from the user's point of view.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-responsive-analysis-loop/02-RESEARCH.md
@.planning/phases/02-responsive-analysis-loop/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Replace the click-only analysis loop with debounced background orchestration</name>
  <files>src/App.tsx</files>
  <action>Update the app shell so draft edits queue background analysis through the Phase 2 scheduler instead of waiting for a click-only snapshot run. Preserve one authoritative draft value in React state, initialize the worker client once, clean it up on unmount, and keep the latest accepted snapshot visible while newer work is queued or running. Do not introduce persistence, multi-document state, or rule-specific warning logic yet.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the app wiring lands so the new orchestration is proven before presentation details are refined.</sampling_rate></verify>
  <done>The app automatically queues fresh local analysis after draft changes, keeps editing responsive, and retains the current draft and last accepted snapshot without page reloads.</done>
</task>

<task type="auto">
  <name>Expose trustworthy queued, running, and refresh-now states in the workspace UI</name>
  <files>src/features/workspace/components/WorkspaceEditor.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx, src/styles.css</files>
  <action>Update the editor and snapshot panels so users can see whether analysis is queued, actively running, current, or failed. Rename the manual action to an immediate-refresh affordance such as `Refresh now`, keep local-only messaging visible, and add accessible live-region copy that explains when the displayed snapshot is stale versus current. Extend the existing visual system with status styling that fits the paper-and-ink direction already established instead of introducing a generic dashboard look.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Inspect the local UI if a status badge or live-region message reads ambiguously after the component update.</manual><sampling_rate>Run after the status UI lands so the visible wording matches the background behavior.</sampling_rate></verify>
  <done>The workspace makes background activity legible through copy, controls, and styling, and users can request an immediate refresh without losing their draft.</done>
</task>

<task type="auto">
  <name>Add end-to-end coverage for debounce timing and fresh-result trust signals</name>
  <files>src/App.test.tsx</files>
  <action>Expand the app workflow tests to cover typing-driven debounce, manual refresh-now behavior, and stale-to-current state transitions. Use fake timers where needed so the debounce window stays deterministic, and assert only user-visible behavior: editor input remains editable, queued/running messages appear at the right times, and the newest analysis result wins after multiple quick edits. Finish by running the full test suite and build to prove the shipped phase stays stable.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>None.</manual><sampling_rate>Run after all Phase 2 UI behavior is wired because this is the phase-complete integration gate.</sampling_rate></verify>
  <done>The project has automated proof that background analysis refreshes after editing pauses, manual refresh still works, and the UI signals freshness accurately in the shipped app.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Draft editing remains responsive while new analysis work is queued or running.
- Analysis refreshes automatically after a short typing pause and can also be triggered immediately without a reload.
- The UI makes analysis freshness and in-progress status explicit, and integration tests prove the loop end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/02-responsive-analysis-loop/02-02-SUMMARY.md`
</output>
