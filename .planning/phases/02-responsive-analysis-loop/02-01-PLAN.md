---
phase: 02-responsive-analysis-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/workspace/types.ts
  - src/features/analysis/types.ts
  - src/features/analysis/workers/localAnalysis.worker.ts
  - src/features/analysis/lib/createAnalysisWorkerClient.ts
  - src/features/analysis/lib/createAnalysisWorkerClient.test.ts
  - src/features/analysis/lib/createAnalysisScheduler.ts
  - src/features/analysis/lib/createAnalysisScheduler.test.ts
autonomous: true
requirements:
  - WORK-03
  - PERF-02
must_haves:
  truths:
    - Local analysis can run away from the typing surface so heavier future checks do not freeze draft editing.
    - Only the newest queued draft can become the accepted analysis result, even if older worker replies arrive later.
  artifacts:
    - { path: src/features/analysis/types.ts, provides: "Typed worker-safe request/result contracts and analysis lifecycle states", contains: "export type AnalysisJobRequest" }
    - { path: src/features/analysis/workers/localAnalysis.worker.ts, provides: "Dedicated worker entry that runs local snapshot analysis off the main thread", contains: "self.onmessage" }
    - { path: src/features/analysis/lib/createAnalysisWorkerClient.ts, provides: "Worker client that posts analysis jobs and resolves typed replies", contains: "new Worker(new URL" }
    - { path: src/features/analysis/lib/createAnalysisScheduler.ts, provides: "Debounced latest-result-wins scheduler for background refreshes", contains: "export function createAnalysisScheduler" }
  key_links:
    - { from: src/features/analysis/lib/createAnalysisWorkerClient.ts, to: src/features/analysis/workers/localAnalysis.worker.ts, via: "Vite module worker construction", pattern: "new Worker(new URL" }
    - { from: src/features/analysis/workers/localAnalysis.worker.ts, to: src/features/workspace/lib/createLocalSnapshot.ts, via: "pure snapshot reuse inside the worker", pattern: "createLocalSnapshot" }
    - { from: src/features/analysis/lib/createAnalysisScheduler.ts, to: src/features/analysis/lib/createAnalysisWorkerClient.ts, via: "debounced dispatch and latest-request tracking", pattern: "requestId" }
---

<objective>
Create the worker-backed analysis foundation that Phase 2 needs before any UI wiring begins.

Purpose: Move analysis execution off the main thread now so the current workspace can stay responsive and Phase 3 can add heavier local rules without re-architecting the pipeline.
Output: Typed worker protocol, dedicated worker client, and a debounced scheduler with deterministic unit coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-responsive-analysis-loop/02-RESEARCH.md
@.planning/phases/01-local-workspace-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Define the background analysis protocol and lifecycle state model</name>
  <files>src/features/workspace/types.ts, src/features/analysis/types.ts</files>
  <action>Create typed request, result, and error payload contracts for dedicated-worker analysis jobs. Extend the shared analysis lifecycle beyond `fresh` and `stale` so later UI work can distinguish `queued`, `running`, `fresh`, and `error` states without guessing. Keep every payload structured-clone-safe and compatible with the existing `LocalSnapshot` shape; do not move any DOM or React concerns into these contracts.</action>
  <verify><automated>npm run build</automated><manual>None.</manual><sampling_rate>Run immediately after the contract update lands so later worker files compile against the same source of truth.</sampling_rate></verify>
  <done>The repo has one typed vocabulary for worker requests, worker replies, and background-analysis status transitions, and the build stays green after the type expansion.</done>
</task>

<task type="auto">
  <name>Implement the dedicated worker entry and typed worker client</name>
  <files>src/features/analysis/workers/localAnalysis.worker.ts, src/features/analysis/lib/createAnalysisWorkerClient.ts, src/features/analysis/lib/createAnalysisWorkerClient.test.ts</files>
  <action>Create a Vite-friendly module worker that imports the existing `createLocalSnapshot` helper and returns typed results tagged with the originating request id. Add a worker client that hides `postMessage`, listens for typed replies, surfaces worker errors cleanly, and owns worker disposal through `terminate()`. Keep the worker persistent across runs instead of recreating it per draft change, and do not add any external worker abstraction library.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/createAnalysisWorkerClient.test.ts</automated><manual>None.</manual><sampling_rate>Run after the worker client and worker entry are wired so request/reply behavior is proven before scheduling logic depends on it.</sampling_rate></verify>
  <done>The app has a dedicated worker-backed client that can send local analysis jobs, receive typed replies, and shut the worker down cleanly when the client is disposed.</done>
</task>

<task type="auto">
  <name>Add the debounced scheduler with latest-result-wins coverage</name>
  <files>src/features/analysis/lib/createAnalysisScheduler.ts, src/features/analysis/lib/createAnalysisScheduler.test.ts</files>
  <action>Build a small scheduler that debounces queued draft changes, exposes an immediate flush path for a manual refresh control, and ignores stale worker results that return after a newer request has already been queued or accepted. Use fake timers and client stubs in the test coverage so debounce timing, cleanup, and stale-result suppression stay deterministic. Keep the delay configurable for future tuning rather than hard-coding magic numbers deep inside `App.tsx`.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/createAnalysisScheduler.test.ts src/features/analysis/lib/createAnalysisWorkerClient.test.ts</automated><manual>None.</manual><sampling_rate>Run after the scheduler lands because later UI work relies on this debounce and stale-result behavior being stable.</sampling_rate></verify>
  <done>The repo has a reusable background-analysis scheduler that can queue, flush, and clean up worker work while guaranteeing that only the newest accepted request can update the UI.</done>
</task>

</tasks>

<verification>
- `npm run build`
- `npm run test -- --run src/features/analysis/lib/createAnalysisWorkerClient.test.ts src/features/analysis/lib/createAnalysisScheduler.test.ts`
</verification>

<success_criteria>
- A dedicated worker boundary exists for local analysis and reuses the current pure snapshot helper.
- The analysis protocol is typed end to end and distinguishes queued, running, fresh, and error states.
- Debounced scheduling and stale-result suppression are covered before UI integration begins.
</success_criteria>

<output>
After completion, create `.planning/phases/02-responsive-analysis-loop/02-01-SUMMARY.md`
</output>
