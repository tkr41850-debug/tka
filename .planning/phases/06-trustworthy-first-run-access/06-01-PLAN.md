---
phase: 06-trustworthy-first-run-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/lib/createDismissedFindingKey.ts
  - src/features/workspace/lib/createAnnouncementMessage.ts
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
autonomous: true
requirements:
  - REVW-04
  - A11Y-02
must_haves:
  truths:
    - Users get one session-owned trust model that can announce important review changes and suppress individual warnings without muting whole rules.
    - The worker and analyzer remain deterministic because dismissals are handled in app state instead of rewriting analysis output upstream.
    - Later tutorial and accessibility work can build on reusable dismissal and announcement helpers instead of scattering logic across components.
  artifacts:
    - { path: src/features/workspace/lib/createDismissedFindingKey.ts, provides: "Stable session dismissal identity for individual findings", contains: "createDismissedFindingKey" }
    - { path: src/features/workspace/lib/createAnnouncementMessage.ts, provides: "Centralized assistive-status copy for review and dismissal events", contains: "createAnnouncementMessage" }
    - { path: src/App.tsx, provides: "App-owned dismissed-finding state and live announcement state", contains: "dismissedFindingKeys" }
    - { path: src/App.test.tsx, provides: "Integration coverage for session dismissal filtering and status announcements", contains: "dismiss" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/lib/createDismissedFindingKey.ts, via: "visible findings filtered against session dismissal keys", pattern: "createDismissedFindingKey" }
    - { from: src/App.tsx, to: src/features/workspace/lib/createAnnouncementMessage.ts, via: "live region copy derived from trust events", pattern: "createAnnouncementMessage" }
    - { from: src/App.test.tsx, to: src/App.tsx, via: "user-visible dismissal and restore assertions", pattern: "dismissedFindingKeys" }
---

<objective>
Build the trust-state foundation before Phase 6 adds a tutorial overlay or final accessibility polish.

Purpose: Add session-only dismissal and announcement primitives so later UI work can rely on one authoritative trust layer without changing the analyzer or introducing persistence.
Output: Stable finding-dismissal helpers, reusable live-announcement copy, app-owned state for both behaviors, and integration coverage that proves one warning can disappear without disabling its rule.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trustworthy-first-run-access/06-RESEARCH.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create session-only dismissal and announcement helpers</name>
  <files>src/features/workspace/lib/createDismissedFindingKey.ts, src/features/workspace/lib/createAnnouncementMessage.ts, src/App.tsx</files>
  <action>Add one helper that derives a stable dismissal key for a visible finding and one helper that produces concise assistive-status copy for selection, dismissal, restore, and refresh events. Introduce app-owned state for dismissed finding keys and the current announcement message, but keep everything session-only and browser-local. Do not add `localStorage`, URL params, or rule-level muting.</action>
  <verify><automated>npm run build</automated><manual>None.</manual><sampling_rate>Run immediately after the trust-state contracts land so later UI work builds against stable helper names and shapes.</sampling_rate></verify>
  <done>The app has one reusable internal language for per-warning dismissal and assistive announcements.</done>
</task>

<task type="auto">
  <name>Filter visible findings by dismissal state without changing analyzer behavior</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx, src/features/workspace/components/ReviewDetailPanel.tsx</files>
  <action>Apply the dismissal keys in the app shell so the visible review list and active detail panel ignore only the dismissed finding instances for the current session. Preserve the underlying analyzer result and rule configuration, automatically clear stale active selection when a dismissed finding disappears, and add initial restore affordances where they belong naturally in the current review surfaces.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after dismissal filtering lands because REVW-04 depends on the UI hiding only the intended warning while leaving the rule active.</sampling_rate></verify>
  <done>The review experience can suppress a single warning in-session without disabling its source rule or mutating the analyzer.</done>
</task>

<task type="auto">
  <name>Add integration coverage for single-warning dismissal and trust announcements</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it proves dismissing one visible finding removes only that warning, keeps same-rule findings available when applicable, and exposes a readable status update or restore affordance after the action. Finish with a build so the new helpers and app wiring stay type-safe across the shipped shell.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full foundation lands because this is the wave-complete gate for REVW-04 groundwork and announcement plumbing.</sampling_rate></verify>
  <done>The repository has automated proof that per-warning dismissals are session-local, reversible, and distinct from rule disablement.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- The app owns session-only dismissal and announcement state in one place.
- Dismissing a warning suppresses only that finding instance in the current review surfaces.
- Automated coverage proves dismissal behavior works without changing analyzer determinism or rule settings.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trustworthy-first-run-access/06-01-SUMMARY.md`
</output>
