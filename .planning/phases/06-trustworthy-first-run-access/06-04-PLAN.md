---
phase: 06-trustworthy-first-run-access
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 03
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
  - src/styles.css
autonomous: true
requirements:
  - REVW-04
  - A11Y-02
must_haves:
  truths:
    - Users can dismiss an individual warning from the shipped review UI and understand how to restore it later in the same session.
    - Dismissal actions feel trustworthy because the interface explains that the rule stays enabled and only the current warning is hidden.
    - Phase 6 closes with end-to-end proof that tutorial, accessibility, and per-warning dismissal behaviors work together through the shipped browser-local flow.
  artifacts:
    - { path: src/features/workspace/components/WorkspaceSnapshot.tsx, provides: "Dismiss or restore affordances within the prioritized review surface", contains: "dismiss" }
    - { path: src/features/workspace/components/ReviewDetailPanel.tsx, provides: "Detail-panel dismissal copy and restore actions for the active warning", contains: "Dismiss warning" }
    - { path: src/App.tsx, provides: "Final session dismissal orchestration and restore handling", contains: "handleDismissFinding" }
    - { path: src/App.test.tsx, provides: "End-to-end coverage for dismissal, restore, tutorial, and trust cues together", contains: "restore" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/components/ReviewDetailPanel.tsx, via: "active-finding dismissal handlers and restore state", pattern: "onDismiss" }
    - { from: src/features/workspace/components/WorkspaceSnapshot.tsx, to: src/App.tsx, via: "list-level dismiss buttons wired to session trust state", pattern: "onDismissFinding" }
    - { from: src/App.test.tsx, to: src/App.tsx, via: "phase-complete dismissal and restore assertions", pattern: "handleRestoreDismissedFindings" }
---

<objective>
Finish Phase 6 with the shipped dismissal workflow and full first-run trust verification.

Purpose: Turn the dismissal groundwork into a visible, understandable, and reversible user-facing workflow that fits the existing review model and completes the phase requirements end to end.
Output: Final dismiss and restore affordances in the review UI, explanatory trust copy, and phase-complete integration coverage plus build verification.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trustworthy-first-run-access/06-RESEARCH.md
@.planning/phases/06-trustworthy-first-run-access/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add dismiss and restore affordances to the shipped review surfaces</name>
  <files>src/features/workspace/components/WorkspaceSnapshot.tsx, src/features/workspace/components/ReviewDetailPanel.tsx, src/App.tsx, src/styles.css</files>
  <action>Expose dismiss actions where users already inspect findings, such as the prioritized list and active detail panel, and add a clear restore path for the current session. Make the copy explicit that dismissal hides this warning only and does not disable its rule. Preserve the existing contextual review flow instead of creating a separate dismissed-items screen.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm dismiss and restore controls remain readable and easy to distinguish on stacked mobile layouts.</manual><sampling_rate>Run after the shipped affordances land because REVW-04 is only complete once users can see and operate the dismissal workflow from the real interface.</sampling_rate></verify>
  <done>The shipped UI lets users dismiss a warning and understand how to bring it back later in the same session.</done>
</task>

<task type="auto">
  <name>Make dismissal behavior explainable and reversible in trust-oriented copy</name>
  <files>src/App.tsx, src/features/workspace/components/ReviewDetailPanel.tsx</files>
  <action>Refine the active-detail and status messaging so dismissal, restore, and still-enabled-rule behavior are obvious. Keep the tone short and direct, announce meaningful outcomes through the live region, and avoid language that implies the warning is deleted permanently or saved between visits.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the copy and announcement refinements land because trust copy is part of the requirement, not a postscript.</sampling_rate></verify>
  <done>The dismissal workflow reads as reversible, local, and rule-safe instead of opaque or destructive.</done>
</task>

<task type="auto">
  <name>Close the phase with end-to-end first-run trust verification</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it covers opening or skipping the tutorial, selecting a finding, dismissing it, confirming the rule remains enabled, restoring the warning, and verifying that visible severity or confidence cues remain explicit throughout. Finish with the full test suite and build.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>Optional local review to confirm tutorial, review, dismiss, and restore interactions stay coherent on desktop and narrow mobile layouts.</manual><sampling_rate>Run after all Phase 6 behavior is wired because this is the phase-complete trust gate for REVW-04, A11Y-01, and A11Y-02 together.</sampling_rate></verify>
  <done>The repository has end-to-end proof that the app's first-run trust features work together through the shipped browser-local workflow.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Users can dismiss and restore an individual warning without disabling its rule.
- The dismissal workflow explains its local, session-only scope clearly.
- Automated coverage proves tutorial, accessibility, and dismissal behaviors work together end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trustworthy-first-run-access/06-04-SUMMARY.md`
</output>
