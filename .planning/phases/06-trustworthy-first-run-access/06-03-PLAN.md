---
phase: 06-trustworthy-first-run-access
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceEditor.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
  - src/features/workspace/components/RuleSettingsPanel.tsx
  - src/styles.css
autonomous: true
requirements:
  - A11Y-02
must_haves:
  truths:
    - Review warnings, settings, and suggestions expose explicit text cues and keyboard-safe interaction paths instead of relying on visual color alone.
    - Dynamic review actions announce important outcomes to assistive technologies without becoming noisy or disruptive.
    - The app's existing editor and review layout stay intact while gaining clearer severity, confidence, and active-state messaging.
  artifacts:
    - { path: src/features/workspace/components/WorkspaceSnapshot.tsx, provides: "Review list with explicit severity and confidence cues beyond color", contains: "finding-severity" }
    - { path: src/features/workspace/components/ReviewDetailPanel.tsx, provides: "Detail surface with accessible dismiss and suggestion copy", contains: "review-detail" }
    - { path: src/features/workspace/components/RuleSettingsPanel.tsx, provides: "Settings surface with stronger helper text and keyboard-friendly labels", contains: "rule-settings" }
    - { path: src/styles.css, provides: "Non-color severity treatments, focus states, and trust-oriented styling", contains: "finding-tone" }
    - { path: src/App.test.tsx, provides: "Integration coverage for explicit review cues and assistive-status updates", contains: "severity" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "visible findings and announcement props", pattern: "analysisState" }
    - { from: src/features/workspace/components/WorkspaceSnapshot.tsx, to: src/features/workspace/components/ReviewDetailPanel.tsx, via: "consistent severity and confidence wording across list and detail", pattern: "confidence" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/RuleSettingsPanel.tsx, via: "keyboard-visible labels and helper copy assertions", pattern: "rule settings" }
---

<objective>
Turn the current review UI into a more explicitly accessible trust surface.

Purpose: Tighten the shipped review, settings, and suggestion flows so keyboard users and assistive-tech users get clear text cues, predictable focus behavior, and meaningful live announcements without replacing the current interface model.
Output: Refined review and settings components, non-color severity and confidence treatments, assistive-status wiring, and integration tests that prove the upgraded trust cues end to end.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trustworthy-first-run-access/06-RESEARCH.md
@.planning/phases/06-trustworthy-first-run-access/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add explicit severity, confidence, and active-state cues across the review UI</name>
  <files>src/features/workspace/components/WorkspaceSnapshot.tsx, src/features/workspace/components/ReviewDetailPanel.tsx, src/styles.css</files>
  <action>Update the finding list and detail panel so severity and heuristic confidence remain understandable without color alone. Pair the existing visual styling with visible text labels or icons, keep the active finding state obvious, and make sure empty, queued, running, and error states still read cleanly to assistive technology users.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the review-surface changes land because A11Y-02 depends on visible and assistive clarity in the warning flow itself.</sampling_rate></verify>
  <done>The review surfaces communicate urgency and confidence explicitly instead of depending on color-only styling.</done>
</task>

<task type="auto">
  <name>Strengthen keyboard and assistive guidance across editor, settings, and suggestion actions</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceEditor.tsx, src/features/workspace/components/RuleSettingsPanel.tsx, src/features/workspace/components/ReviewDetailPanel.tsx</files>
  <action>Thread the new live-announcement helper through meaningful interaction points such as selecting a finding, applying a suggestion, undoing a rewrite, dismissing a warning, and restoring one. Tighten labels and helper copy where the settings and suggestion surfaces are still too implicit, but preserve native controls instead of replacing them with custom widgets.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after announcement and helper-copy updates land because they are easy to regress while wiring multiple review actions together.</sampling_rate></verify>
  <done>The app announces important review events politely and exposes clearer keyboard-friendly labels across the trust-critical surfaces.</done>
</task>

<task type="auto">
  <name>Add integration coverage for explicit trust cues across warnings, settings, and suggestions</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it proves visible severity or confidence cues exist outside of color, meaningful status text updates after review actions, and the settings plus suggestion controls remain reachable by standard keyboard-driven interaction patterns. Finish with a build to keep the upgraded trust surfaces type-safe.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full accessibility polish lands because this is the wave-complete gate for A11Y-02.</sampling_rate></verify>
  <done>The repository has automated proof that the trust-critical review surfaces expose explicit, accessible cues beyond the current color treatment.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Severity and confidence remain understandable without color alone.
- Review, settings, and suggestion actions produce helpful assistive-status updates.
- Keyboard users can operate the trust-critical surfaces through native controls and predictable focus behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trustworthy-first-run-access/06-03-SUMMARY.md`
</output>
