---
phase: 06-trustworthy-first-run-access
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/FirstRunTutorial.tsx
  - src/styles.css
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
must_haves:
  truths:
    - Users can understand highlights, the review list, and rule settings through a short tutorial they can skip or finish quickly.
    - The tutorial behaves like an accessible dialog with focus moved into it and returned on close.
    - Users can reopen the tutorial during the same session instead of being trapped by a one-time experience.
  artifacts:
    - { path: src/features/workspace/components/FirstRunTutorial.tsx, provides: "Accessible first-run quick tour dialog with clear skip and finish actions", contains: "export function FirstRunTutorial" }
    - { path: src/App.tsx, provides: "Session tutorial state, close handlers, and reopen affordance", contains: "isTutorialOpen" }
    - { path: src/App.test.tsx, provides: "Integration coverage for tutorial skip, finish, and reopen flows", contains: "tutorial" }
    - { path: src/styles.css, provides: "Responsive tutorial overlay styling that matches the existing editorial UI", contains: "tutorial" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/components/FirstRunTutorial.tsx, via: "controlled open or close state and step metadata", pattern: "FirstRunTutorial" }
    - { from: src/features/workspace/components/FirstRunTutorial.tsx, to: src/App.tsx, via: "skip, next, and finish callbacks", pattern: "onClose" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/FirstRunTutorial.tsx, via: "user-visible modal and focus assertions", pattern: "dialog" }
---

<objective>
Deliver the guided first-run tour for the existing workspace without introducing persistence or a heavyweight product-tour dependency.

Purpose: Teach the three key surfaces that already exist in the app - highlights, prioritized review, and live rule settings - through a quick tutorial that respects keyboard and screen-reader users.
Output: A dedicated first-run tutorial component, app wiring for session-first open and manual reopen behavior, responsive styling, and integration tests for skip and complete flows.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trustworthy-first-run-access/06-RESEARCH.md
@.planning/phases/06-trustworthy-first-run-access/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create the first-run tutorial dialog and step content</name>
  <files>src/features/workspace/components/FirstRunTutorial.tsx, src/styles.css</files>
  <action>Build a short guided tutorial dialog that explains the highlight layer, prioritized review, and live rule settings in plain language. Use accessible dialog semantics, keep the copy brief enough to finish in under a minute, and provide explicit next, skip, and finish actions rather than hidden gestures.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm the tutorial remains readable on narrow mobile widths and does not dominate the workspace visually.</manual><sampling_rate>Run after the tutorial shell lands so modal visibility and accessible names are verified before app-level focus return logic depends on it.</sampling_rate></verify>
  <done>The app has a concrete first-run quick tour component that matches the current product language and layout.</done>
</task>

<task type="auto">
  <name>Wire session-first open, skip, finish, and reopen behavior into the app shell</name>
  <files>src/App.tsx</files>
  <action>Open the tutorial automatically on first render for the current session, move focus into the dialog, and return focus cleanly when the dialog closes. Add a visible reopen affordance somewhere natural in the shipped shell so users can revisit the tour after skipping or finishing it. Keep completion state in memory only; do not add persistence before Phase 7.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the app wiring lands because A11Y-01 depends on the actual first-run flow and focus return, not just the dialog markup.</sampling_rate></verify>
  <done>The first-run tour opens at the right time, closes cleanly, and remains available later in the same session.</done>
</task>

<task type="auto">
  <name>Add integration coverage for tutorial completion, skip, and reopen paths</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it proves the tutorial appears on first load, can be skipped or completed, and can be reopened from the main interface afterward. Include at least one focus-oriented assertion that the dialog exposes a recognizable role and does not strand keyboard users when it closes, then finish with a build.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full tutorial flow lands because this is the wave-complete trust gate for A11Y-01.</sampling_rate></verify>
  <done>The repository has automated proof that the quick tour is accessible, skippable, and reusable within the current session.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Users can complete or skip a brief tutorial that introduces the app's three core review surfaces.
- The tutorial uses accessible dialog semantics with predictable focus entry and exit.
- Users can reopen the tutorial later in the same session without persistence yet.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trustworthy-first-run-access/06-02-SUMMARY.md`
</output>
