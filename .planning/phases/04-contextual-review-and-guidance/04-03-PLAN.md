---
phase: 04-contextual-review-and-guidance
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/lib/applySuggestedRewrite.ts
  - src/styles.css
autonomous: true
requirements:
  - GUID-02
  - GUID-03
  - REVW-03
must_haves:
  truths:
    - User can see targeted rewrite suggestions or examples for supported rules from the active review surface.
    - User can apply a safe suggested rewrite and immediately undo that change without leaving the current workflow.
    - The editor, active finding state, and background analysis refresh stay synchronized after apply and undo actions.
  artifacts:
    - { path: src/features/workspace/components/ReviewDetailPanel.tsx, provides: "Guidance UI with apply-example-or-rewrite actions for the active finding", contains: "Apply suggestion" }
    - { path: src/App.tsx, provides: "App orchestration for safe rewrite apply, one-step undo, and immediate reanalysis", contains: "undoRewrite" }
    - { path: src/features/workspace/lib/applySuggestedRewrite.ts, provides: "Finalized exact-range apply and undo helper used by the app shell", contains: "undoSnapshot" }
    - { path: src/App.test.tsx, provides: "Integration coverage for apply suggestion, undo, and post-edit refresh behavior", contains: "undo" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/lib/applySuggestedRewrite.ts, via: "safe exact-range rewrite orchestration", pattern: "applySuggestedRewrite" }
    - { from: src/App.tsx, to: src/features/workspace/components/ReviewDetailPanel.tsx, via: "active suggestion actions and undo affordance", pattern: "onApplySuggestion" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/ReviewDetailPanel.tsx, via: "user-visible guidance and undo assertions", pattern: "Apply suggestion" }
---

<objective>
Finish Phase 4 by making supported guidance actionable and reversible.

Purpose: Turn deterministic suggestion metadata into a safe apply-and-undo flow that feels trustworthy, stays local, and keeps the review context synchronized after edits.
Output: Review actions for supported rules, one-step undo behavior in the app shell, and end-to-end tests that prove contextual review still works after rewrite application.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contextual-review-and-guidance/04-RESEARCH.md
@.planning/phases/04-contextual-review-and-guidance/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Expose supported rewrite suggestions and examples in the active review surface</name>
  <files>src/features/workspace/components/ReviewDetailPanel.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx</files>
  <action>Expand the active review UI so supported rules can show either a safe direct replacement action or an example-only suggestion, depending on the guidance payload from analysis. Keep the language explicit about what is certain versus what is illustrative, and do not show an `Apply` button for heuristic findings that lack exact replacement metadata.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the guidance actions land so visible suggestion affordances stay aligned with the analysis contracts from plan 01.</sampling_rate></verify>
  <done>The active review surface shows targeted rewrite guidance for supported rules and clearly distinguishes direct apply from example-only help.</done>
</task>

<task type="auto">
  <name>Wire safe suggestion apply, one-step undo, and immediate reanalysis</name>
  <files>src/App.tsx, src/features/workspace/lib/applySuggestedRewrite.ts</files>
  <action>Use the Phase 4 rewrite helper to apply supported suggestions against the current draft, preserve or restore selection context, and store one undo snapshot for the most recent rewrite action. After every apply or undo, queue immediate local analysis so stale findings disappear and the active finding state updates safely. Keep the feature intentionally narrow: one-step undo only, no persisted edit history, and no bulk-apply actions.</action>
  <verify><automated>npm run test -- --run src/features/workspace/lib/applySuggestedRewrite.test.ts src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the app orchestration lands because the correctness of apply and undo depends on both the pure helper and the user-visible workflow.</sampling_rate></verify>
  <done>The app can safely apply one supported suggestion, undo it once, and refresh contextual analysis immediately after each action.</done>
</task>

<task type="auto">
  <name>Add end-to-end coverage for suggestion apply, undo, and contextual recovery</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so a supported CLAR-05 or filler-style finding can be selected, explained, applied, and undone from the shipped UI. Assert that the draft text changes exactly as expected, the active review context stays coherent, and a fresh analysis result replaces stale findings after each action. Finish by running the full suite and build as the phase-complete gate.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>Optional local review to confirm apply and undo controls remain easy to discover on stacked mobile layouts.</manual><sampling_rate>Run after all Phase 4 actions are wired because this is the phase-complete gate for guidance trust and contextual continuity.</sampling_rate></verify>
  <done>The project has end-to-end proof that supported rewrite guidance can be applied and undone safely without breaking contextual review behavior.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Supported findings expose targeted rewrite suggestions or examples through the active review surface.
- Users can apply a safe exact-range rewrite and undo it once without leaving the current workflow.
- Integration coverage proves apply, undo, and post-edit contextual refresh behavior end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/04-contextual-review-and-guidance/04-03-SUMMARY.md`
</output>
