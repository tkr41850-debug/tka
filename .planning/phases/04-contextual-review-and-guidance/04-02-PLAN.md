---
phase: 04-contextual-review-and-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceEditor.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
  - src/features/workspace/lib/createHighlightedDraftSegments.ts
  - src/styles.css
autonomous: true
requirements:
  - REVW-01
  - REVW-03
  - GUID-01
must_haves:
  truths:
    - User can see the exact issue span called out inline while preserving the existing textarea editing model.
    - User can move from the prioritized review list to the matching text and back without losing active review context.
    - User can open an active finding and read a short explanation in a dedicated detail surface instead of only scanning the list.
  artifacts:
    - { path: src/features/workspace/lib/createHighlightedDraftSegments.ts, provides: "Draft-to-highlight segment builder for inline review rendering", contains: "export function createHighlightedDraftSegments" }
    - { path: src/features/workspace/components/WorkspaceEditor.tsx, provides: "Textarea-first editor with mirrored highlight surface and active finding selection", contains: "activeFinding" }
    - { path: src/features/workspace/components/ReviewDetailPanel.tsx, provides: "Focused review detail surface with explanation copy for the active finding", contains: "export function ReviewDetailPanel" }
    - { path: src/App.tsx, provides: "App orchestration for active finding state, editor focus, and list-to-text navigation", contains: "activeFindingId" }
    - { path: src/App.test.tsx, provides: "Integration coverage for inline highlight and issue navigation flows", contains: "setSelectionRange" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceEditor.tsx, via: "active finding props and focus-navigation callbacks", pattern: "activeFindingId" }
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "review-list activation and detail selection", pattern: "onSelectFinding" }
    - { from: src/features/workspace/components/WorkspaceEditor.tsx, to: src/features/workspace/lib/createHighlightedDraftSegments.ts, via: "inline highlight rendering from finding offsets", pattern: "createHighlightedDraftSegments" }
---

<objective>
Turn Phase 4's richer findings into a contextual review experience that stays anchored to the existing editor.

Purpose: Add inline issue emphasis, bidirectional issue navigation, and a focused explanation surface without abandoning the product's one-textarea workflow.
Output: Active finding orchestration in the app shell, a mirrored highlight layer in the editor, a reusable review detail panel, and integration coverage for list-to-text navigation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contextual-review-and-guidance/04-RESEARCH.md
@.planning/phases/04-contextual-review-and-guidance/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Wire active finding state and list-to-text navigation through the app shell</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx</files>
  <action>Introduce app-level active-finding state that lets the review list select a finding, keeps that selection stable while analysis is current, and clears it safely when newer analysis removes the finding. Add callbacks so a list item can request navigation into the editor, but keep the existing Phase 2 freshness model and Phase 3 prioritized findings surface intact.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the orchestration lands so finding selection and stale-result handling are proven before inline visuals are added.</sampling_rate></verify>
  <done>The app can track one active finding, activate it from the review list, and preserve contextual review state across background refreshes where possible.</done>
</task>

<task type="auto">
  <name>Render inline issue highlights in the textarea-first editor</name>
  <files>src/features/workspace/components/WorkspaceEditor.tsx, src/features/workspace/lib/createHighlightedDraftSegments.ts, src/styles.css</files>
  <action>Build a mirrored highlight presentation layer that uses finding offsets to underline exact spans and emphasize the active issue while leaving the native textarea as the real editable control. Sync typography, spacing, and scrolling closely enough that highlights stay believable on long drafts, and use programmatic textarea focus plus selection ranges when a finding is activated. Do not introduce a rich-text editor or contenteditable surface.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Inspect the local UI on desktop and a narrow mobile width to confirm highlight alignment and active-span visibility.</manual><sampling_rate>Run after the inline review surface lands because the navigation flow is not trustworthy until the visible editor context matches the selected finding.</sampling_rate></verify>
  <done>The editor shows exact issue spans inline through a mirrored review layer while preserving native textarea editing and selection behavior.</done>
</task>

<task type="auto">
  <name>Add the focused explanation panel and navigation coverage</name>
  <files>src/features/workspace/components/ReviewDetailPanel.tsx, src/App.test.tsx, src/styles.css</files>
  <action>Create a compact review detail surface that shows the active finding's rule label, short explanation, location, and matched excerpt. Keep the list scannable and let the user move between the list, editor, and detail panel without losing context. Expand integration tests so they prove a finding can be selected from the list, the editor selection follows it, the explanation appears, and the current review context survives a non-destructive refresh cycle.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>Optional local review to confirm the explanation panel remains readable beside the editor on wide and stacked layouts.</manual><sampling_rate>Run after the full contextual-review UI lands because this is the wave-complete trust gate for inline review and navigation.</sampling_rate></verify>
  <done>The shipped UI lets users inspect one issue in context through inline highlights, list-to-text navigation, and a short explanation surface.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Inline highlights or underlines mark exact issue spans without replacing the textarea editing model.
- Users can move from the issue list to the matching text and maintain active review context.
- A focused explanation surface exposes why the active warning fired.
</success_criteria>

<output>
After completion, create `.planning/phases/04-contextual-review-and-guidance/04-02-SUMMARY.md`
</output>
