---
phase: 04-contextual-review-and-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis/types.ts
  - src/features/analysis/lib/findings/detectComplexWording.ts
  - src/features/analysis/lib/analyzeDraft.ts
  - src/features/analysis/lib/analyzeDraft.test.ts
  - src/features/workspace/lib/applySuggestedRewrite.ts
  - src/features/workspace/lib/applySuggestedRewrite.test.ts
autonomous: true
requirements:
  - GUID-01
  - GUID-02
  - CLAR-05
must_haves:
  truths:
    - User can see why a supported warning fired because findings carry concise explanation copy and optional suggestion metadata.
    - User can receive plainer alternatives for supported jargon or overly complex wording through deterministic local guidance.
    - The repo has one safe exact-range rewrite helper and one-step undo contract before UI actions depend on it.
  artifacts:
    - { path: src/features/analysis/types.ts, provides: "Finding guidance contracts with stable ids and optional safe-apply suggestions", contains: "suggestions" }
    - { path: src/features/analysis/lib/findings/detectComplexWording.ts, provides: "Deterministic jargon and complex-wording detection with simpler alternatives", contains: "export function detectComplexWording" }
    - { path: src/features/analysis/lib/analyzeDraft.ts, provides: "Aggregate analyzer that merges CLAR-05 guidance into existing findings", contains: "detectComplexWording" }
    - { path: src/features/workspace/lib/applySuggestedRewrite.ts, provides: "Exact-range apply and one-step undo helper for safe suggestions", contains: "export function applySuggestedRewrite" }
    - { path: src/features/workspace/lib/applySuggestedRewrite.test.ts, provides: "Deterministic coverage for apply and undo behavior", contains: "describe('applySuggestedRewrite'" }
  key_links:
    - { from: src/features/analysis/lib/analyzeDraft.ts, to: src/features/analysis/lib/findings/detectComplexWording.ts, via: "aggregate detector composition for jargon guidance", pattern: "detectComplexWording" }
    - { from: src/features/workspace/lib/applySuggestedRewrite.test.ts, to: src/features/workspace/lib/applySuggestedRewrite.ts, via: "exact-range replacement and undo coverage", pattern: "applySuggestedRewrite" }
---

<objective>
Build the guidance and safe-edit foundation that the contextual Phase 4 UI will consume.

Purpose: Extend the Phase 3 findings model so explanations, rewrite suggestions, and deterministic complex-wording guidance all exist before editor highlighting and navigation are wired into the app.
Output: Richer finding contracts, a local jargon-or-complexity detector with simpler alternatives, and a reusable safe-apply-plus-undo helper with focused unit coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-contextual-review-and-guidance/04-RESEARCH.md
@.planning/phases/03-core-findings/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Expand finding contracts for explanation and suggestion metadata</name>
  <files>src/features/analysis/types.ts</files>
  <action>Extend the shared finding model with a stable id and optional guidance payloads that can express short explanation copy, example rewrites, safe replacement text, and whether a suggestion can be auto-applied. Keep everything serializable for the existing worker protocol, and do not push review-state flags such as active selection into worker data.</action>
  <verify><automated>npm run build</automated><manual>None.</manual><sampling_rate>Run immediately after the type update so later analyzer and UI files compile against one guidance contract.</sampling_rate></verify>
  <done>The repo has one typed vocabulary for navigable findings, explanation copy, and safe suggestion metadata, and the build stays green.</done>
</task>

<task type="auto">
  <name>Add deterministic complex-wording detection with simpler alternatives</name>
  <files>src/features/analysis/lib/findings/detectComplexWording.ts, src/features/analysis/lib/analyzeDraft.ts, src/features/analysis/lib/analyzeDraft.test.ts</files>
  <action>Create a local detector for a curated pack of jargon and overly complex wording such as `utilize`, `leverage`, `facilitate`, `robust`, and other clearly replaceable phrases. Emit CLAR-05 findings with concise explanation copy and plainer alternatives in the suggestion payload, then expand analyzer fixtures so the new guidance appears in stable order beside the existing Phase 3 rule pack.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts</automated><manual>None.</manual><sampling_rate>Run after the new detector lands because later UI work assumes explanation and suggestion payloads already exist for supported rules.</sampling_rate></verify>
  <done>The aggregate analyzer can flag supported complex wording, explain why it was flagged, and return plainer alternatives through deterministic local metadata.</done>
</task>

<task type="auto">
  <name>Create the safe suggestion apply and undo helper</name>
  <files>src/features/workspace/lib/applySuggestedRewrite.ts, src/features/workspace/lib/applySuggestedRewrite.test.ts</files>
  <action>Build a small helper that applies an exact-range replacement, returns the updated draft and selection range, and records the minimum metadata needed for a one-step undo. Cover successful replacements, stale-offset rejection, and undo restoration with unit tests. Keep the helper pure so Phase 4 UI components can call it without embedding string-manipulation logic in React event handlers.</action>
  <verify><automated>npm run test -- --run src/features/workspace/lib/applySuggestedRewrite.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run after the helper and tests land because later review actions depend on safe exact-range replacement staying deterministic.</sampling_rate></verify>
  <done>The repo has a tested exact-range rewrite primitive with one-step undo support, ready for the app shell to use in later plans.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts src/features/workspace/lib/applySuggestedRewrite.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Findings can carry concise explanation text plus suggestion metadata without breaking the worker protocol.
- Supported jargon and complex wording rules emit simpler alternatives through deterministic local analysis.
- A pure helper exists for safe exact-range apply and one-step undo before UI actions depend on it.
</success_criteria>

<output>
After completion, create `.planning/phases/04-contextual-review-and-guidance/04-01-SUMMARY.md`
</output>
