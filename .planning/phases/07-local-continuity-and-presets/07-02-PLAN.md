---
phase: 07-local-continuity-and-presets
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
  - src/features/workspace/lib/createAnnouncementMessage.ts
autonomous: true
requirements:
  - SAVE-01
must_haves:
  truths:
    - Same-browser visits restore the user's live tuning, saved dismissals, and tutorial continuity without adding accounts or remote state.
    - Hydration happens through one app-owned continuity flow that preserves the existing worker-backed analysis model instead of persisting derived results.
    - Return-state messaging stays trustworthy because the UI explains what was restored and what remains local to this browser.
  artifacts:
    - { path: src/App.tsx, provides: "App-shell hydration and persistence orchestration for saved continuity state", contains: "loadWorkspacePersistence" }
    - { path: src/features/workspace/components/WorkspaceSnapshot.tsx, provides: "Visible continuity summary for restored settings and saved local state", contains: "saved locally" }
    - { path: src/features/workspace/components/ReviewDetailPanel.tsx, provides: "Trust copy that reflects persisted dismissal behavior without implying remote sync", contains: "same browser" }
    - { path: src/App.test.tsx, provides: "Integration coverage for restored settings, dismissals, and tutorial continuity", contains: "restores" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/lib/createAnnouncementMessage.ts, via: "hydration and persistence events announced through the shared trust channel", pattern: "session-restored" }
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "restored continuity state summarized in the shipped UI", pattern: "dismissedCount" }
    - { from: src/App.test.tsx, to: src/App.tsx, via: "same-browser restoration assertions for settings and dismissals", pattern: "analysisSettings" }
---

<objective>
Hydrate and persist same-browser continuity behavior before Phase 7 adds preset management and draft recovery UI.

Purpose: Restore saved rule settings, dismissal state, and tutorial continuity on return visits while keeping the existing local analysis workflow authoritative and understandable.
Output: App-shell hydration plus save orchestration, continuity-aware status copy, and integration coverage for restored same-browser state.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-continuity-and-presets/07-RESEARCH.md
@.planning/phases/07-local-continuity-and-presets/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Hydrate saved settings, dismissals, and tutorial continuity in the app shell</name>
  <files>src/App.tsx, src/features/workspace/lib/createAnnouncementMessage.ts</files>
  <action>Load persisted same-browser continuity during app initialization so saved rule settings, saved dismissal keys, and first-run tutorial completion or reopen state restore without a post-load jump. Recompute fresh analysis from restored source state through the existing scheduler path and announce meaningful restore outcomes through the shared live-region helper.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after hydration lands because SAVE-01 depends on startup restoration behaving correctly before later preset and recovery work builds on it.</sampling_rate></verify>
  <done>Return visits restore saved browser-local tuning and trust continuity through one app-owned flow.</done>
</task>

<task type="auto">
  <name>Persist continuity changes and surface trustworthy same-browser copy</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx, src/features/workspace/components/ReviewDetailPanel.tsx</files>
  <action>Write continuity updates back to browser storage whenever settings, dismissals, or tutorial state changes, and adjust the shipped UI copy so users understand these preferences stay on the same browser only. Preserve the current dismissal semantics that hide a warning instance while keeping its rule enabled, and avoid language that implies account-based sync.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm restored-state copy remains readable on narrow layouts.</manual><sampling_rate>Run after persistence wiring and copy refinements land because same-browser continuity is only trustworthy if the UI explains it clearly.</sampling_rate></verify>
  <done>The app both saves and clearly explains return-state continuity without overstating what is remembered.</done>
</task>

<task type="auto">
  <name>Add integration coverage for restored settings, dismissals, and tutorial state</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it proves a second app render on the same browser can restore saved rule toggles, numeric thresholds, custom banned phrases, dismissed warnings, and tutorial completion or reopen behavior. Include at least one scenario where stale dismissal keys are pruned after the restored draft produces different findings.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full same-browser continuity flow lands because this is the wave-complete gate for SAVE-01 groundwork.</sampling_rate></verify>
  <done>The repository has automated proof that same-browser continuity restores only valid local state and keeps trust cues intact.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Same-browser visits restore saved settings, dismissals, and tutorial continuity.
- The app persists source state and recomputes fresh analysis instead of restoring stale results.
- Automated coverage proves restored local state stays valid and understandable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-continuity-and-presets/07-02-SUMMARY.md`
</output>
