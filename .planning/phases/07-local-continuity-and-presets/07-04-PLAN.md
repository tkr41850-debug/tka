---
phase: 07-local-continuity-and-presets
plan: 04
type: execute
wave: 4
depends_on:
  - 02
  - 03
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceEditor.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/components/RuleSettingsPanel.tsx
  - src/styles.css
autonomous: true
requirements:
  - SAVE-02
  - SAVE-01
must_haves:
  truths:
    - Draft recovery is available for same-browser return visits only when the user explicitly enables it.
    - Restoring a saved draft is a visible choice, not a surprise replacement of the current workspace.
    - Phase 7 closes with end-to-end proof that settings continuity, presets, dismissals, and optional draft recovery work together through the shipped browser-local flow.
  artifacts:
    - { path: src/App.tsx, provides: "Recovery enablement, restore/discard orchestration, and phase-complete continuity flow", contains: "handleRestoreSavedDraft" }
    - { path: src/features/workspace/components/WorkspaceEditor.tsx, provides: "Workspace recovery prompt or actions near the editing surface", contains: "Restore saved draft" }
    - { path: src/features/workspace/components/RuleSettingsPanel.tsx, provides: "User-facing recovery preference control with local-only explanation", contains: "draft recovery" }
    - { path: src/App.test.tsx, provides: "Phase-complete integration coverage for explicit draft recovery and saved continuity together", contains: "restore saved draft" }
  key_links:
    - { from: src/features/workspace/components/RuleSettingsPanel.tsx, to: src/App.tsx, via: "recovery preference toggles authoritative persistence behavior", pattern: "onToggleDraftRecovery" }
    - { from: src/features/workspace/components/WorkspaceEditor.tsx, to: src/App.tsx, via: "restore and discard actions target the saved draft state", pattern: "onRestoreSavedDraft" }
    - { from: src/App.test.tsx, to: src/App.tsx, via: "explicit recovery and discard assertions across same-browser renders", pattern: "savedDraft" }
---

<objective>
Finish Phase 7 with explicit draft recovery and full local continuity verification.

Purpose: Add the last missing return-flow behavior by making draft recovery opt-in, visible, and reversible while proving that it works alongside saved presets, settings, dismissals, and tutorial continuity.
Output: Recovery preference controls, restore-versus-discard draft UI, and phase-complete integration coverage plus build verification.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-continuity-and-presets/07-RESEARCH.md
@.planning/phases/07-local-continuity-and-presets/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add a local-only draft recovery preference and explicit restore prompt</name>
  <files>src/App.tsx, src/features/workspace/components/RuleSettingsPanel.tsx, src/features/workspace/components/WorkspaceEditor.tsx, src/styles.css</files>
  <action>Introduce a clear recovery preference that explains drafts can be remembered on this browser only, and when enabled, show a visible restore-versus-discard prompt if a saved draft exists on a later visit. Keep the prompt near the editing surface, include enough metadata to make the choice understandable, and avoid replacing the current workspace automatically before the user decides.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm the recovery prompt remains clear and unobtrusive on desktop and narrow mobile layouts.</manual><sampling_rate>Run after the recovery UI lands because SAVE-02 depends on the restore choice being both explicit and usable.</sampling_rate></verify>
  <done>The app offers privacy-sensitive draft recovery without surprising returning users.</done>
</task>

<task type="auto">
  <name>Persist and clear saved drafts according to the enabled recovery policy</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx</files>
  <action>Save draft continuity only when recovery is enabled, clear stored draft content when the user disables recovery or discards the saved draft, and surface concise local-only status copy so users understand what is remembered between visits. Preserve the existing worker-backed refresh behavior by rerunning analysis after restore instead of persisting stale findings.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after recovery persistence wiring lands because the privacy-sensitive policy is only real if the stored draft clears when users opt out.</sampling_rate></verify>
  <done>Draft persistence follows the user's explicit recovery choice and can be discarded cleanly.</done>
</task>

<task type="auto">
  <name>Close the phase with end-to-end local continuity verification</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it covers enabling recovery, writing a draft, returning on the same browser, choosing restore or discard, confirming saved settings and presets still restore, and verifying dismissals remain same-browser local. Finish with the full test suite and build.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>Optional local review to confirm the full continuity flow feels coherent across desktop and mobile layouts.</manual><sampling_rate>Run after all Phase 7 behavior is wired because this is the phase-complete gate for SAVE-01, SAVE-02, and RULE-04 together.</sampling_rate></verify>
  <done>The repository has end-to-end proof that same-browser continuity and optional draft recovery work together through the shipped local workflow.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Draft recovery is opt-in and restore is an explicit choice.
- Disabling recovery or discarding a saved draft clears stored draft content.
- Automated coverage proves presets, settings continuity, dismissals, and optional recovery work together end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-continuity-and-presets/07-04-SUMMARY.md`
</output>
