---
phase: 07-local-continuity-and-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis/types.ts
  - src/features/workspace/lib/browserPersistence.ts
  - src/features/workspace/lib/browserPersistence.test.ts
  - src/features/analysis/lib/normalizeAnalysisSettings.ts
autonomous: true
requirements:
  - RULE-04
  - SAVE-01
  - SAVE-02
must_haves:
  truths:
    - The app has one versioned browser-local continuity contract instead of scattered ad hoc `localStorage` keys.
    - Saved presets and saved session state both reuse normalized, serializable settings shapes that are already safe for the worker pipeline.
    - Corrupt or stale browser data never blocks startup because storage reads validate and fall back to safe defaults.
  artifacts:
    - { path: src/features/workspace/lib/browserPersistence.ts, provides: "Versioned save and load helpers for continuity state and presets", contains: "saveWorkspacePersistence" }
    - { path: src/features/workspace/lib/browserPersistence.test.ts, provides: "Unit coverage for parse safety, normalization, and versioned storage behavior", contains: "browser persistence" }
    - { path: src/features/analysis/types.ts, provides: "Typed preset and continuity payload contracts that stay JSON-safe", contains: "SavedRulePreset" }
    - { path: src/features/analysis/lib/normalizeAnalysisSettings.ts, provides: "Stable normalization reused before presets and persistence are written", contains: "normalizeAnalysisSettings" }
  key_links:
    - { from: src/features/workspace/lib/browserPersistence.ts, to: src/features/analysis/lib/normalizeAnalysisSettings.ts, via: "saved settings and presets normalized before storage", pattern: "normalizeAnalysisSettings" }
    - { from: src/features/workspace/lib/browserPersistence.test.ts, to: src/features/workspace/lib/browserPersistence.ts, via: "schema and fallback assertions for versioned storage", pattern: "loadWorkspacePersistence" }
---

<objective>
Build the persistence foundation before Phase 7 wires continuity into the shipped UI.

Purpose: Create one versioned browser-local storage layer and typed contracts for presets plus saved session state so later app-shell work can hydrate and persist safely without duplicating schema logic.
Output: Serializable continuity types, versioned storage helpers, normalization-aware persistence behavior, and unit coverage for parse safety plus fallback handling.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-continuity-and-presets/07-RESEARCH.md
@.planning/phases/06-trustworthy-first-run-access/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Define the typed browser-local continuity and preset contracts</name>
  <files>src/features/analysis/types.ts, src/features/workspace/lib/browserPersistence.ts</files>
  <action>Add serializable types for saved rule presets, recovery metadata, and the full browser-local continuity envelope. Keep the shape JSON-safe, versioned, and narrow: normalized settings, dismissal keys, tutorial continuity, recovery preference, optional saved draft metadata, and preset records only.</action>
  <verify><automated>npm run test -- --run src/features/workspace/lib/browserPersistence.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run immediately after the contract lands so all later continuity tasks build on one saved-state vocabulary.</sampling_rate></verify>
  <done>The repo has one typed continuity schema for saved local state and presets.</done>
</task>

<task type="auto">
  <name>Create versioned load and save helpers with defensive fallback behavior</name>
  <files>src/features/workspace/lib/browserPersistence.ts, src/features/analysis/lib/normalizeAnalysisSettings.ts</files>
  <action>Implement browser-local helpers that read and write a versioned storage envelope, normalize saved settings before persisting or restoring them, prune obviously invalid data, and fall back cleanly when storage is missing, corrupt, or outdated. Keep the API small and app-shell friendly instead of exposing raw `localStorage` reads throughout the codebase.</action>
  <verify><automated>npm run test -- --run src/features/workspace/lib/browserPersistence.test.ts</automated><manual>None.</manual><sampling_rate>Run after the storage helper lands because SAVE-01 and RULE-04 both depend on reliable persistence primitives.</sampling_rate></verify>
  <done>The app has one safe browser-persistence helper boundary instead of scattered ad hoc storage calls.</done>
</task>

<task type="auto">
  <name>Add fast unit coverage for versioning, parse safety, and preset normalization</name>
  <files>src/features/workspace/lib/browserPersistence.test.ts</files>
  <action>Cover successful save and load behavior, malformed JSON fallback, unknown-version fallback, normalized preset restoration, and a recovery-disabled path that drops any stored draft payload from the restored session. Finish with a build so the persistence contracts remain type-safe.</action>
  <verify><automated>npm run test -- --run src/features/workspace/lib/browserPersistence.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full persistence foundation lands because this is the wave-complete gate for the storage layer before UI wiring begins.</sampling_rate></verify>
  <done>The repository has fast proof that browser-local continuity data loads safely and predictably.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/features/workspace/lib/browserPersistence.test.ts`
- `npm run build`
</verification>

<success_criteria>
- The repo exposes one versioned browser-local persistence helper boundary.
- Saved settings and presets use normalized, serializable contracts.
- Unit coverage proves corrupt or stale local data falls back safely.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-continuity-and-presets/07-01-SUMMARY.md`
</output>
