---
phase: 07-local-continuity-and-presets
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/RuleSettingsPanel.tsx
  - src/styles.css
autonomous: true
requirements:
  - RULE-04
  - SAVE-01
must_haves:
  truths:
    - Users can save reusable local rule presets on the same browser and apply them without rebuilding settings manually.
    - Presets use the same normalized settings contract as live analysis, so applying one immediately affects findings through the existing refresh loop.
    - Preset management stays lightweight and local by extending the current rule-settings surface instead of creating a new navigation flow.
  artifacts:
    - { path: src/features/workspace/components/RuleSettingsPanel.tsx, provides: "Preset save, apply, rename, and delete controls inside the shipped settings panel", contains: "preset" }
    - { path: src/App.tsx, provides: "App handlers for preset CRUD and apply flows", contains: "handleSavePreset" }
    - { path: src/App.test.tsx, provides: "Integration coverage for local preset management and apply behavior", contains: "preset" }
    - { path: src/styles.css, provides: "Preset-management layout and responsive styling", contains: "preset" }
  key_links:
    - { from: src/features/workspace/components/RuleSettingsPanel.tsx, to: src/App.tsx, via: "preset CRUD callbacks bound to authoritative settings and persistence state", pattern: "onSavePreset" }
    - { from: src/App.tsx, to: src/features/workspace/components/RuleSettingsPanel.tsx, via: "saved presets rendered and applied in the existing settings surface", pattern: "savedPresets" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/RuleSettingsPanel.tsx, via: "user-visible preset save and apply assertions", pattern: "Save preset" }
---

<objective>
Turn Phase 7 continuity into a reusable preset workflow inside the shipped settings UI.

Purpose: Let users save, apply, rename, and delete browser-local rule presets without leaving the current workspace or adding a separate preferences screen.
Output: Preset management controls in the rule-settings panel, app-level preset handlers, and integration coverage that proves preset application refreshes the current analysis.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-continuity-and-presets/07-RESEARCH.md
@.planning/phases/07-local-continuity-and-presets/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add lightweight preset management controls to the current rule-settings panel</name>
  <files>src/features/workspace/components/RuleSettingsPanel.tsx, src/styles.css</files>
  <action>Extend the shipped settings panel with a compact preset area that can save the current normalized settings under a name, list saved presets, apply one preset, rename it, and delete it. Reject blank or duplicate names case-insensitively, keep the controls keyboard-friendly, and preserve the existing local-only tone.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm the preset controls stay readable without crowding the existing settings stack on mobile.</manual><sampling_rate>Run after the preset UI lands because RULE-04 becomes user-visible here first.</sampling_rate></verify>
  <done>The shipped settings surface can manage local presets without introducing a second settings workflow.</done>
</task>

<task type="auto">
  <name>Wire preset save and apply flows through the authoritative app state</name>
  <files>src/App.tsx, src/features/workspace/components/RuleSettingsPanel.tsx</files>
  <action>Implement preset create, apply, rename, and delete handlers in the app shell so presets persist on the same browser, applying one updates the current settings immediately, and the analysis loop refreshes against the selected preset. Preserve manual edits after a preset apply instead of forcing a locked preset mode.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after preset wiring lands because saved settings are only valuable if applying them changes the current review deterministically.</sampling_rate></verify>
  <done>Users can store reusable tuning setups locally and swap between them in the live workspace.</done>
</task>

<task type="auto">
  <name>Add end-to-end preset coverage for save, apply, rename, and delete behavior</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it proves saving the current settings as a preset, applying a preset on a later render, renaming it, deleting it, and seeing the active findings refresh accordingly. Include one case where a preset changes both a rule toggle and a numeric threshold so the test covers more than cosmetic storage behavior.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full preset workflow lands because this is the wave-complete gate for RULE-04.</sampling_rate></verify>
  <done>The repository has automated proof that browser-local rule presets are reusable and affect live analysis correctly.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Users can save, apply, rename, and delete local rule presets on the same browser.
- Presets reuse the existing normalized settings contract and refresh live analysis immediately.
- Automated coverage proves preset behavior works across return visits and live review updates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-continuity-and-presets/07-03-SUMMARY.md`
</output>
