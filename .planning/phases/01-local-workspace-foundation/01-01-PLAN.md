---
phase: 01-local-workspace-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.test.tsx
  - src/styles.css
  - src/lib/bootMetrics.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - User can open a branded static page and land in a ready workspace shell without waiting on a server round-trip.
    - User sees the app identify itself as browser-local and ready for the current document.
  artifacts:
    - { path: package.json, provides: "Static-site scripts and dependency manifest for local development, build, and tests", contains: '"build": "tsc --noEmit && vite build"' }
    - { path: vite.config.ts, provides: "Vite and Vitest configuration for a fast static React app", contains: "plugins: [react()]" }
    - { path: src/App.tsx, provides: "App shell, layout, and readiness messaging", contains: "Technical Writing Assistant" }
    - { path: src/styles.css, provides: "Responsive design tokens and editorial layout styling", contains: ":root" }
  key_links:
    - { from: src/main.tsx, to: src/App.tsx, via: "createRoot render", pattern: "createRoot.*App" }
    - { from: src/main.tsx, to: src/styles.css, via: "global stylesheet import", pattern: "import './styles.css'" }
---

<objective>
Establish the static-site delivery shell and a fast, testable React foundation for the assistant.

Purpose: Give every later phase a stable browser-only entry point, shared visual language, and automated build/test commands without introducing server dependencies.
Output: Vite React TypeScript app shell, Vitest wiring, boot-latency indicator, and responsive global styling.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-workspace-foundation/01-RESEARCH.md
@.planning/phases/01-local-workspace-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Scaffold the Vite React TypeScript static app</name>
  <files>package.json, package-lock.json, tsconfig.json, vite.config.ts, index.html, src/main.tsx, src/test/setup.ts</files>
  <action>Create a root Vite React TypeScript project configured for static hosting. Add dev, build, preview, and test scripts; install React and testing dependencies; and configure Vitest with a jsdom environment in the shared Vite config. Keep the project browser-only: no SSR, no API routes, no telemetry packages, and no cloud SDKs.</action>
  <verify><automated>npm run test -- --run</automated><manual>Confirm the test runner boots without missing-environment errors.</manual><sampling_rate>Run immediately after the scaffold task lands.</sampling_rate></verify>
  <done>The repo has a working package manifest, Vite/Vitest config, and a passing initial test run in the root app.</done>
</task>

<task type="auto">
  <name>Build the branded app shell and readiness surface</name>
  <files>src/App.tsx, src/styles.css, src/lib/bootMetrics.ts</files>
  <action>Replace the default starter UI with an editorial landing shell for Technical Writing Assistant. Use CSS variables, warm paper-inspired surfaces, a responsive two-column layout, and a visible local-only status area that includes a measured ready-state indicator. Keep styling dependency-free and load fast with plain CSS rather than a UI framework.</action>
  <verify><automated>npm run build</automated><manual>Inspect the build output if asset sizes look unexpectedly heavy for a small shell.</manual><sampling_rate>Run after the shell and styling update.</sampling_rate></verify>
  <done>The app opens into a distinctive responsive shell that signals browser-local execution and reports a measured ready-state indicator.</done>
</task>

<task type="auto">
  <name>Add shell smoke coverage for startup readiness</name>
  <files>src/App.test.tsx</files>
  <action>Add a lightweight render test that proves the root heading, local-only copy, and ready-state badge appear when the app loads. Keep the test user-facing by querying the DOM the way a reader would, not by reaching into component internals.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the smoke test is added.</sampling_rate></verify>
  <done>The project has automated evidence that the branded static shell renders and exposes readiness messaging on load.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Static app shell builds successfully from the repo root.
- Ready-state messaging appears without any network or account requirements.
- The foundational UI is responsive, styled, and covered by at least one render smoke test.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-workspace-foundation/01-01-SUMMARY.md`
</output>
