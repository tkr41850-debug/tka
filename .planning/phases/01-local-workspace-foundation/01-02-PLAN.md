---
phase: 01-local-workspace-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/types.ts
  - src/features/workspace/data/sampleDraft.ts
  - src/features/workspace/components/WorkspaceEditor.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/features/workspace/lib/createLocalSnapshot.ts
  - src/features/workspace/lib/createLocalSnapshot.test.ts
autonomous: true
requirements:
  - WORK-01
  - WORK-02
must_haves:
  truths:
    - User can paste or type text into a single document workspace.
    - User can replace the current draft with starter content or clear it without leaving the page.
    - User can run a local analysis pass and see updated snapshot details without uploads, accounts, or remote processing.
  artifacts:
    - { path: src/features/workspace/components/WorkspaceEditor.tsx, provides: "Single-source text input surface and workspace controls", contains: "textarea" }
    - { path: src/features/workspace/lib/createLocalSnapshot.ts, provides: "Browser-local snapshot analysis for the current draft", contains: "export function createLocalSnapshot" }
    - { path: src/features/workspace/components/WorkspaceSnapshot.tsx, provides: "Analysis status and current snapshot metrics", contains: "Local snapshot" }
    - { path: src/App.tsx, provides: "State wiring between the editor and the analysis snapshot panel", contains: "Run local snapshot" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceEditor.tsx, via: "props for value, onChange, and workspace actions", pattern: "WorkspaceEditor" }
    - { from: src/App.tsx, to: src/features/workspace/lib/createLocalSnapshot.ts, via: "analyze button handler", pattern: "createLocalSnapshot" }
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "snapshot prop handoff", pattern: "WorkspaceSnapshot" }
---

<objective>
Turn the branded shell into a working single-text workspace with a local analysis entry point.

Purpose: Satisfy the product's first real user value by letting someone paste, type, replace, and analyze one draft entirely in the browser.
Output: Workspace editor, starter draft actions, pure local snapshot analysis, and automated tests for the draft-to-analysis flow.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-workspace-foundation/01-RESEARCH.md
@.planning/phases/01-local-workspace-foundation/01-VALIDATION.md
@.planning/phases/01-local-workspace-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement the single-document workspace surface</name>
  <files>src/App.tsx, src/features/workspace/types.ts, src/features/workspace/data/sampleDraft.ts, src/features/workspace/components/WorkspaceEditor.tsx</files>
  <action>Create a feature-owned workspace module with one controlled `textarea`, clear and replace actions, and accessible labels that make paste, typing, and replacement obvious. Keep exactly one authoritative draft value in React state. Do not add persistence, multiple documents, or background sync yet.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the editor surface and controls are wired.</sampling_rate></verify>
  <done>The page exposes a single source editor that can accept typed or pasted text and can replace or clear that text without reloading.</done>
</task>

<task type="auto">
  <name>Wire in browser-local snapshot analysis</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx, src/features/workspace/lib/createLocalSnapshot.ts</files>
  <action>Add a pure local snapshot helper that derives word, sentence, paragraph, and reading-time metrics from the current draft. Wire the analyze button so the current text is processed in-browser and the results panel updates with explicit local-only messaging. Keep the output intentionally light; richer warning logic belongs to later phases.</action>
  <verify><automated>npm run test -- --run src/features/workspace/lib/createLocalSnapshot.test.ts</automated><manual>None.</manual><sampling_rate>Run after the snapshot helper and results panel are added.</sampling_rate></verify>
  <done>Running local analysis updates the UI with current draft metrics and makes it clear that processing stayed inside the browser.</done>
</task>

<task type="auto">
  <name>Add end-to-end workspace flow coverage</name>
  <files>src/App.test.tsx, src/features/workspace/lib/createLocalSnapshot.test.ts</files>
  <action>Expand the smoke test into a workflow test that covers typing or replacing content, triggering local analysis, and reading the updated snapshot text. Add focused unit coverage for `createLocalSnapshot` edge cases such as empty text and multi-paragraph input.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>None.</manual><sampling_rate>Run after all Phase 1 behaviors are wired.</sampling_rate></verify>
  <done>The project has automated evidence that the workspace and local snapshot flow work together in the shipped UI.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- A single document workspace is visible and editable on first load.
- Replace and clear actions operate on that one workspace without page reloads.
- Local analysis can run on the current draft and surface updated snapshot details with no remote dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-workspace-foundation/01-02-SUMMARY.md`
</output>
