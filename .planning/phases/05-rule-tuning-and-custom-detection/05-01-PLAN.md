---
phase: 05-rule-tuning-and-custom-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis/types.ts
  - src/features/analysis/lib/defaultAnalysisSettings.ts
  - src/features/analysis/lib/normalizeAnalysisSettings.ts
  - src/features/analysis/lib/findings/detectLengthAndWordiness.ts
  - src/features/analysis/lib/findings/detectVoiceAndTense.ts
  - src/features/analysis/lib/findings/detectComplexWording.ts
  - src/features/analysis/lib/findings/detectCustomBannedPhrases.ts
  - src/features/analysis/lib/analyzeDraft.ts
  - src/features/analysis/lib/analyzeDraft.test.ts
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - CLAR-07
must_haves:
  truths:
    - Users can change which rules are active because the analyzer accepts an explicit settings object instead of assuming hard-coded defaults.
    - Users can tighten or relax sentence and paragraph limits and get findings that respect those numbers.
    - Users can supply custom banned phrases and receive deterministic findings with exact spans and clear explanations.
  artifacts:
    - { path: src/features/analysis/lib/defaultAnalysisSettings.ts, provides: "Default rule settings and labels for the Phase 5 rule pack", contains: "DEFAULT_ANALYSIS_SETTINGS" }
    - { path: src/features/analysis/lib/normalizeAnalysisSettings.ts, provides: "Worker-safe normalization for enabled rules, numeric thresholds, and custom phrases", contains: "normalizeAnalysisSettings" }
    - { path: src/features/analysis/lib/findings/detectCustomBannedPhrases.ts, provides: "Deterministic custom banned phrase detector with exact location metadata", contains: "detectCustomBannedPhrases" }
    - { path: src/features/analysis/lib/analyzeDraft.ts, provides: "Aggregate analyzer that composes configurable rule detectors", contains: "settings" }
    - { path: src/features/analysis/lib/analyzeDraft.test.ts, provides: "Unit coverage for toggles, thresholds, and custom banned phrases", contains: "custom banned phrase" }
  key_links:
    - { from: src/features/analysis/lib/analyzeDraft.ts, to: src/features/analysis/lib/normalizeAnalysisSettings.ts, via: "normalized settings applied before detector composition", pattern: "normalizeAnalysisSettings" }
    - { from: src/features/analysis/lib/analyzeDraft.ts, to: src/features/analysis/lib/findings/detectCustomBannedPhrases.ts, via: "aggregate detector composition for custom phrase findings", pattern: "detectCustomBannedPhrases" }
    - { from: src/features/analysis/lib/analyzeDraft.test.ts, to: src/features/analysis/lib/analyzeDraft.ts, via: "rule toggle, threshold, and banned phrase assertions", pattern: "analyzeDraft" }
---

<objective>
Build the configurable analysis foundation before Phase 5 exposes any settings controls.

Purpose: Replace hard-coded detector assumptions with one normalized settings contract so toggles, thresholds, and custom banned phrases all work through the existing local analysis layer and current findings UI.
Output: Shared rule settings contracts, normalized defaults, a deterministic custom banned phrase detector, and analyzer tests that prove Phase 5 behaviors before worker or UI wiring begins.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-RESEARCH.md
@.planning/phases/04-contextual-review-and-guidance/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create the shared Phase 5 analysis settings contract and normalization helpers</name>
  <files>src/features/analysis/types.ts, src/features/analysis/lib/defaultAnalysisSettings.ts, src/features/analysis/lib/normalizeAnalysisSettings.ts</files>
  <action>Define one serializable `AnalysisSettings` contract that covers per-rule enablement, supported numeric thresholds, and custom banned phrases. Add defaults and a normalization helper that clamps numeric values, fills in missing toggles, trims whitespace, and dedupes banned phrases case-insensitively. Keep the shape worker-safe: plain objects, arrays, strings, numbers, and booleans only.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run immediately after the shared settings contract lands so later detector work compiles against one authoritative configuration shape.</sampling_rate></verify>
  <done>The repo has one typed and normalized rule-settings vocabulary ready for detector composition and worker transport.</done>
</task>

<task type="auto">
  <name>Refactor the analyzer to respect toggles, thresholds, and custom banned phrases</name>
  <files>src/features/analysis/lib/findings/detectLengthAndWordiness.ts, src/features/analysis/lib/findings/detectVoiceAndTense.ts, src/features/analysis/lib/findings/detectComplexWording.ts, src/features/analysis/lib/findings/detectCustomBannedPhrases.ts, src/features/analysis/lib/analyzeDraft.ts</files>
  <action>Update the existing rule detectors so they accept normalized settings and skip work when their rule is disabled. Replace the hard-coded sentence and paragraph limits with configurable values, keep the existing complex-wording and heuristic rules switchable, and add a deterministic `detectCustomBannedPhrases` helper that matches normalized whole phrases case-insensitively while preserving exact finding offsets and clear CLAR-07 explanation copy.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts</automated><manual>None.</manual><sampling_rate>Run after detector composition changes because custom phrase coverage and threshold behavior must be proven before worker and UI plumbing can trust the results.</sampling_rate></verify>
  <done>The analyzer can turn each supported rule on or off, honor configurable limits, and emit deterministic custom banned phrase findings through the existing finding contract.</done>
</task>

<task type="auto">
  <name>Add analyzer coverage for configurable rule behavior</name>
  <files>src/features/analysis/lib/analyzeDraft.test.ts</files>
  <action>Expand the unit suite so it proves disabled rules stop emitting findings, sentence and paragraph limits react to new thresholds, and custom banned phrases produce deterministic matches with stable location data. Cover at least one normalization path such as duplicate phrase removal or whitespace cleanup so later UI work can rely on sanitized settings behavior.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full analysis foundation lands because this is the wave-complete gate for configurable detector correctness.</sampling_rate></verify>
  <done>The project has fast automated proof that Phase 5 rule configuration changes produce the intended analysis outcomes before UI wiring starts.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts`
- `npm run build`
</verification>

<success_criteria>
- The analyzer accepts one normalized settings object instead of relying on hard-coded rule behavior.
- Supported rule toggles and numeric thresholds change findings deterministically.
- Custom banned phrases produce standard deterministic findings with exact spans and explanations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rule-tuning-and-custom-detection/05-01-SUMMARY.md`
</output>
