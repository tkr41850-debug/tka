---
phase: 05-rule-tuning-and-custom-detection
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/RuleSettingsPanel.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/styles.css
autonomous: true
requirements:
  - RULE-01
  - RULE-02
must_haves:
  truths:
    - Users can turn supported rules on and off individually from the shipped UI.
    - Users can change sentence and paragraph thresholds from the shipped UI and see the review update live.
    - The settings surface remains readable beside the current editor and review workflow instead of feeling bolted on.
  artifacts:
    - { path: src/features/workspace/components/RuleSettingsPanel.tsx, provides: "Dedicated settings UI for per-rule toggles and numeric thresholds", contains: "export function RuleSettingsPanel" }
    - { path: src/App.tsx, provides: "Settings handlers and prop wiring for the shipped controls", contains: "RuleSettingsPanel" }
    - { path: src/App.test.tsx, provides: "Integration coverage for toggle and threshold interactions", contains: "checkbox" }
    - { path: src/styles.css, provides: "Responsive styling for the settings surface within the existing editorial UI", contains: "rule-settings" }
  key_links:
    - { from: src/App.tsx, to: src/features/workspace/components/RuleSettingsPanel.tsx, via: "controlled props and change handlers for the active settings object", pattern: "onToggleRule" }
    - { from: src/features/workspace/components/RuleSettingsPanel.tsx, to: src/App.tsx, via: "controlled checkbox and number input updates", pattern: "type=\"checkbox\"" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/RuleSettingsPanel.tsx, via: "user-visible assertions for toggles and thresholds", pattern: "Rule settings" }
---

<objective>
Expose Phase 5 rule toggles and numeric thresholds through a deliberate settings surface.

Purpose: Turn the settings-aware analysis foundation into a user-facing control panel that fits the existing workspace layout and keeps live review behavior explicit.
Output: A dedicated rule settings component, app wiring for controlled inputs, responsive styling, and integration coverage for per-rule enablement plus threshold tuning.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-RESEARCH.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create the dedicated rule settings panel for toggles and thresholds</name>
  <files>src/features/workspace/components/RuleSettingsPanel.tsx, src/styles.css</files>
  <action>Build a dedicated settings component that lists the supported rules with concise labels, short helper copy, and one checkbox per rule. Add numeric controls for the sentence-word and paragraph-sentence limits using controlled number inputs, and keep the design aligned with the existing paper-and-ink interface rather than switching to generic admin-form styling.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm the settings surface stays readable on desktop and a narrow mobile width.</manual><sampling_rate>Run after the panel structure lands so controlled inputs and accessible labels are verified before the rest of the sidebar layout shifts around them.</sampling_rate></verify>
  <done>The app has a dedicated rule settings panel with accessible controlled toggles and threshold inputs that match the existing visual language.</done>
</task>

<task type="auto">
  <name>Integrate the settings panel into the app shell and current review summary</name>
  <files>src/App.tsx, src/features/workspace/components/WorkspaceSnapshot.tsx, src/styles.css</files>
  <action>Mount the settings panel in the main workflow so it sits naturally beside the editor and review surfaces. Surface a compact summary of the active tuning state where it helps orient the user, but do not duplicate the full control set in multiple places. Keep live refresh messaging explicit so users understand the review is updating to match their latest settings.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after app integration because the rule settings surface is only trustworthy once it is visibly connected to the active review flow.</sampling_rate></verify>
  <done>The settings panel feels like part of the shipped workspace, and the analysis surface communicates that findings respect the current tuning state.</done>
</task>

<task type="auto">
  <name>Add integration coverage for per-rule toggles and numeric threshold tuning</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it proves individual rules can be disabled and re-enabled, sentence and paragraph thresholds can be changed from the UI, and the visible findings list updates to match the new settings. Finish with a build so the new component and app wiring stay type-safe across the workspace.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full settings UI lands because this is the wave-complete gate for RULE-01 and RULE-02.</sampling_rate></verify>
  <done>The project has end-to-end proof that the shipped settings controls can toggle rules and retune supported thresholds live.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Users can enable or disable each supported rule from the UI.
- Users can change supported numeric thresholds and see the review update live.
- The settings surface remains coherent with the current editor and contextual review layout.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rule-tuning-and-custom-detection/05-03-SUMMARY.md`
</output>
