---
phase: 05-rule-tuning-and-custom-detection
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/RuleSettingsPanel.tsx
  - src/features/workspace/components/ReviewDetailPanel.tsx
  - src/styles.css
autonomous: true
requirements:
  - RULE-03
  - CLAR-07
must_haves:
  truths:
    - Users can add and remove custom banned phrases from the active browser session.
    - Users can see custom banned phrase findings in the existing contextual review without a special-case workflow.
    - The full app proves custom phrase tuning works end to end without introducing persistence or remote processing.
  artifacts:
    - { path: src/features/workspace/components/RuleSettingsPanel.tsx, provides: "Custom banned phrase editor with add and remove controls", contains: "custom banned phrase" }
    - { path: src/features/workspace/components/ReviewDetailPanel.tsx, provides: "Detail copy that explains custom banned phrase findings inside the current review surface", contains: "finding.ruleLabel" }
    - { path: src/App.tsx, provides: "App handlers for phrase creation, removal, and immediate settings-driven analysis refresh", contains: "customBannedPhrases" }
    - { path: src/App.test.tsx, provides: "End-to-end coverage for adding phrases, detecting matches, and removing phrases", contains: "banned phrase" }
  key_links:
    - { from: src/features/workspace/components/RuleSettingsPanel.tsx, to: src/App.tsx, via: "phrase add/remove callbacks bound to the authoritative settings state", pattern: "onAddPhrase" }
    - { from: src/App.tsx, to: src/features/workspace/components/ReviewDetailPanel.tsx, via: "custom phrase findings rendered through the existing detail workflow", pattern: "customBannedPhrases" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/RuleSettingsPanel.tsx, via: "user-visible custom phrase editing assertions", pattern: "custom banned phrase" }
---

<objective>
Finish Phase 5 with live custom banned phrase editing and full rule-tuning verification.

Purpose: Deliver the last missing Phase 5 behavior by letting users define their own banned phrases in-session and see those phrases flow through the existing highlight, list, and detail review surfaces.
Output: A controlled custom phrase editor, review detail support for the new finding type, and phase-complete integration coverage plus build verification.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-RESEARCH.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add the custom banned phrase editor to the shipped settings surface</name>
  <files>src/features/workspace/components/RuleSettingsPanel.tsx, src/App.tsx, src/styles.css</files>
  <action>Extend the rule settings panel with a small custom banned phrase editor that lets users add a phrase, remove an existing phrase, and understand that the list applies only to the current in-memory session. Validate obvious bad input such as blank entries or duplicate phrases, but keep the interaction lightweight and do not add persistence, import/export, or preset management.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm the add/remove controls remain clear on stacked mobile layouts.</manual><sampling_rate>Run after the editor lands so phrase editing behavior is verified before the final review-surface polish and full-suite gate.</sampling_rate></verify>
  <done>The shipped UI lets users manage a small in-session custom banned phrase list without leaving the current workspace.</done>
</task>

<task type="auto">
  <name>Carry custom banned phrase findings through the existing contextual review surfaces</name>
  <files>src/features/workspace/components/ReviewDetailPanel.tsx, src/App.tsx</files>
  <action>Make sure custom banned phrase findings read clearly in the existing review detail workflow and continue to use the same highlight, list, and active-finding interactions introduced in Phase 4. Preserve the current local-only messaging, and avoid introducing apply-suggestion actions for this rule unless the analyzer already has an exact safe replacement to offer.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the review-surface wiring because CLAR-07 is only complete once custom phrases behave like first-class findings in the shipped UI.</sampling_rate></verify>
  <done>Custom banned phrase findings appear and read naturally inside the current contextual review flow without needing a separate review mode.</done>
</task>

<task type="auto">
  <name>Close the phase with end-to-end custom phrase and rule-tuning verification</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration suite so it covers adding a custom phrase, seeing the matching finding appear in the review UI, removing the phrase, and confirming the finding disappears after a fresh analysis. Include one phase-complete scenario that exercises a rule toggle, a numeric threshold change, and custom banned phrase detection together, then finish with the full test suite and build.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>Optional local review to confirm settings, findings, and detail copy remain coherent on desktop and narrow mobile layouts.</manual><sampling_rate>Run after all Phase 5 behavior is wired because this is the phase-complete trust gate for RULE-03 and CLAR-07.</sampling_rate></verify>
  <done>The repository has end-to-end proof that live rule tuning and custom banned phrase detection work together through the shipped browser-local workflow.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- Users can add and remove custom banned phrases from the current session.
- Custom banned phrase matches appear in the same contextual review flow as other findings.
- Automated coverage proves toggles, thresholds, and custom phrases work together end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rule-tuning-and-custom-detection/05-04-SUMMARY.md`
</output>
