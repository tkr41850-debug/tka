---
phase: 05-rule-tuning-and-custom-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/features/analysis/types.ts
  - src/features/analysis/lib/createAnalysisWorkerClient.ts
  - src/features/analysis/lib/createAnalysisScheduler.ts
  - src/features/analysis/workers/localAnalysis.worker.ts
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
must_haves:
  truths:
    - Users can change rule settings and know the latest analysis reflects both the latest draft and the latest settings.
    - Settings changes reuse the existing local worker pipeline instead of blocking typing or adding a second analysis path.
    - Active review context clears safely when a settings change removes the currently selected finding.
  artifacts:
    - { path: src/features/analysis/lib/createAnalysisWorkerClient.ts, provides: "Worker client that sends settings with each analysis request", contains: "settings" }
    - { path: src/features/analysis/workers/localAnalysis.worker.ts, provides: "Worker entry point that analyzes draft text against normalized settings", contains: "analyzeDraft" }
    - { path: src/App.tsx, provides: "App-owned rule settings state and refresh orchestration", contains: "analysisSettings" }
    - { path: src/App.test.tsx, provides: "Integration coverage for settings-driven reanalysis and active finding cleanup", contains: "settings" }
  key_links:
    - { from: src/App.tsx, to: src/features/analysis/lib/createAnalysisScheduler.ts, via: "queue and flush calls that include draft plus latest settings", pattern: "queue(" }
    - { from: src/features/analysis/lib/createAnalysisWorkerClient.ts, to: src/features/analysis/workers/localAnalysis.worker.ts, via: "typed request payload containing rule settings", pattern: "analysis/request" }
    - { from: src/App.test.tsx, to: src/App.tsx, via: "user-visible settings updates that trigger fresh analysis", pattern: "findings" }
---

<objective>
Thread configurable rule settings through the worker-backed analysis loop and app shell.

Purpose: Keep Phase 2 freshness guarantees intact while making the latest draft and latest rule settings travel together through every background analysis request.
Output: Updated worker request types, settings-aware client and worker plumbing, app-level settings state, and integration tests that prove settings changes safely refresh contextual review.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-RESEARCH.md
@.planning/phases/05-rule-tuning-and-custom-detection/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Extend the worker protocol so every analysis request carries the latest settings</name>
  <files>src/features/analysis/types.ts, src/features/analysis/lib/createAnalysisWorkerClient.ts, src/features/analysis/lib/createAnalysisScheduler.ts, src/features/analysis/workers/localAnalysis.worker.ts</files>
  <action>Update the typed request and scheduler seams so `queue()` and `flush()` accept both draft text and the current normalized settings. Preserve the existing latest-result-wins lifecycle and error handling, and keep request payloads structured-clone-safe. The worker should call the configurable analyzer from plan 01 instead of reintroducing hard-coded detector defaults.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the worker protocol changes so settings transport is proven before the app shell starts mutating rule state.</sampling_rate></verify>
  <done>The background analysis pipeline can send and receive settings-aware requests without regressing freshness or worker error behavior.</done>
</task>

<task type="auto">
  <name>Add app-level rule settings state and reanalysis orchestration</name>
  <files>src/App.tsx</files>
  <action>Introduce one app-owned `analysisSettings` state initialized from the Phase 5 defaults. Update draft queue and flush calls so they always use the latest settings, trigger a fresh background analysis when settings change, and keep the existing active finding cleanup logic so selections clear when a now-disabled or no-longer-matching finding disappears.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run after the app shell orchestration lands because stale active-finding behavior becomes much easier to miss once settings can invalidate findings.</sampling_rate></verify>
  <done>The app owns one authoritative settings object, re-runs local analysis when it changes, and preserves or clears contextual review state safely.</done>
</task>

<task type="auto">
  <name>Cover settings-driven refresh behavior in the app integration suite</name>
  <files>src/App.test.tsx</files>
  <action>Expand the integration tests so they prove toggling a rule or changing a threshold leads to updated findings in the shipped UI without breaking the worker-backed freshness model. Include at least one case where the active finding disappears after a settings change so the review detail surface and editor selection recover cleanly. Finish with a build to confirm the updated request types compile across the app.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx && npm run build</automated><manual>None.</manual><sampling_rate>Run after the full protocol-and-app wiring lands because this is the wave-complete trust gate for settings-aware reanalysis.</sampling_rate></verify>
  <done>The repo has automated proof that settings changes refresh the live review safely through the existing worker pipeline.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Worker requests carry the latest settings alongside the latest draft.
- Changing rule settings triggers fresh local analysis without replacing the current latest-result-wins behavior.
- Active review context recovers safely when settings remove or add findings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rule-tuning-and-custom-detection/05-02-SUMMARY.md`
</output>
