---
phase: 08-ci-and-github-pages-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - README.md
  - .github/workflows/ci-pages.yml
  - src/App.test.tsx
autonomous: true
requirements:
  - CI-01
  - CI-02
  - DEPLOY-01
  - DEPLOY-02
must_haves:
  truths:
    - Maintainers know exactly which repository settings are required for Pages and merge protection.
    - Manual redeploy remains available without changing code, and the repo documents how to use it.
    - Phase 8 closes with verification that CI quality gates, Pages deployment wiring, and the visible release label all work together coherently.
  artifacts:
    - { path: README.md, provides: "Repository setup and release-path instructions for CI, Pages, branch protection, and manual redeploy", contains: "GitHub Pages" }
    - { path: .github/workflows/ci-pages.yml, provides: "Final workflow naming, summaries, and deploy visibility polish", contains: "workflow_dispatch" }
    - { path: src/App.test.tsx, provides: "Phase-complete verification that the app still renders the release label and core shell after deployment wiring", contains: "build version" }
  key_links:
    - { from: README.md, to: .github/workflows/ci-pages.yml, via: "repo docs reference the exact workflow job names and manual dispatch path", pattern: "ci-pages.yml" }
    - { from: src/App.test.tsx, to: src/App.tsx, via: "phase-complete assertions protect the release label and core shell together", pattern: "version" }
---

<objective>
Finish Phase 8 with a maintainable, documented release path.

Purpose: Close the gap between working YAML and a usable release process by documenting the required GitHub settings, preserving manual redeploy behavior, and running the full verification gate that proves CI plus Pages deployment is ready to maintain.
Output: Repository release docs, final workflow visibility polish, and phase-complete verification across lint, tests, and build.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ci-and-github-pages-deployment/08-CONTEXT.md
@.planning/phases/08-ci-and-github-pages-deployment/08-RESEARCH.md
@.planning/phases/08-ci-and-github-pages-deployment/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Document the required GitHub Pages and branch-protection setup</name>
  <files>README.md</files>
  <action>Create or extend repository documentation with the exact setup steps maintainers need after merge: enable Pages via GitHub Actions, require the named `lint`, `test`, and `build` checks on `main`, and understand the manual redeploy trigger. Keep the instructions short, operational, and aligned with the checked-in workflow file.</action>
  <verify><automated>npm run lint</automated><manual>Read the new docs once end to end and confirm they mention Pages source, required checks, and manual redeploy explicitly.</manual><sampling_rate>Run after the docs update because CI-02 and DEPLOY-02 are not operationally complete if the repository settings remain tribal knowledge.</sampling_rate></verify>
  <done>The release path is documented clearly enough for another maintainer to configure and operate it.</done>
</task>

<task type="auto">
  <name>Polish workflow summaries and final deploy visibility</name>
  <files>.github/workflows/ci-pages.yml</files>
  <action>Add concise workflow summary output or final step naming so successful and failed deploy outcomes are easy to read in GitHub Actions, while preserving the separate job structure and manual dispatch support. Keep the output informative but small.</action>
  <verify><automated>npm run lint && npm run test -- --run && npm run build</automated><manual>Inspect the workflow file to confirm deployment remains a separate visible job and still supports manual dispatch without code changes.</manual><sampling_rate>Run after the final workflow polish so failure visibility is checked under the same commands the workflow advertises.</sampling_rate></verify>
  <done>Deploy status stays easy to spot in GitHub without sacrificing the clean job layout from earlier plans.</done>
</task>

<task type="auto">
  <name>Close the phase with full CI and Pages verification</name>
  <files>src/App.test.tsx</files>
  <action>Refresh any integration assertions needed so the app shell, release label, and core local workspace still pass after the deployment-oriented changes, then run the full lint, test, and build gate. Treat this as the final proof that the repository is ready to ship from GitHub Pages without regressing the product itself.</action>
  <verify><automated>npm run lint && npm run test -- --run && npm run build</automated><manual>After merge, run one manual GitHub Actions deploy and open the live Pages site to confirm the version label and production URL both look correct.</manual><sampling_rate>Run at the end of the plan because this is the full phase-complete gate for CI-01, CI-02, DEPLOY-01, and DEPLOY-02 together.</sampling_rate></verify>
  <done>The repository has end-to-end proof that CI and Pages deployment are release-ready.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- The repo documents required GitHub Pages and branch-protection settings.
- Manual redeploy remains available and visible.
- Full lint, test, and build verification closes the phase without regressing the shipped app.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-and-github-pages-deployment/08-03-SUMMARY.md`
</output>
