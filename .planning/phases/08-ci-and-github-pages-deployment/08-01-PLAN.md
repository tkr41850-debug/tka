---
phase: 08-ci-and-github-pages-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - .github/workflows/ci-pages.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
must_haves:
  truths:
    - Every pull request and push can run explicit `lint`, `test`, and `build` checks in GitHub Actions.
    - The repository exposes stable check names that can be required before `main` merges.
    - The CI foundation stays deterministic and repo-local instead of depending on hidden GitHub defaults.
  artifacts:
    - { path: package.json, provides: "Named npm scripts for CI quality gates", contains: "lint" }
    - { path: eslint.config.js, provides: "Flat ESLint config for TypeScript and React app code", contains: "defineConfig" }
    - { path: .github/workflows/ci-pages.yml, provides: "Named GitHub Actions jobs for lint, test, and build on push plus pull request", contains: "jobs:" }
  key_links:
    - { from: .github/workflows/ci-pages.yml, to: package.json, via: "workflow jobs call the repo's canonical npm scripts", pattern: "npm run lint" }
    - { from: eslint.config.js, to: package.json, via: "lint script resolves the checked-in flat config", pattern: "eslint" }
---

<objective>
Create the CI foundation before Pages deployment work begins.

Purpose: Add the missing lint gate, expose stable repo scripts, and create a clearly named GitHub Actions workflow so every push and pull request can show explicit quality outcomes that are safe to require before merge.
Output: ESLint flat config, npm lint wiring, and a workflow with visible `lint`, `test`, and `build` jobs for CI.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ci-and-github-pages-deployment/08-CONTEXT.md
@.planning/phases/08-ci-and-github-pages-deployment/08-RESEARCH.md
@.planning/phases/07-local-continuity-and-presets/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add the repo lint command and minimal flat ESLint configuration</name>
  <files>package.json, package-lock.json, eslint.config.js</files>
  <action>Install and configure the smallest reliable ESLint setup for the current React + TypeScript app, then expose it through a stable `npm run lint` script. Favor deterministic correctness-focused rules over style-heavy churn so the new CI gate starts green with minimal noise.</action>
  <verify><automated>npm run lint</automated><manual>None.</manual><sampling_rate>Run as soon as lint lands because every later CI task depends on a stable lint command existing first.</sampling_rate></verify>
  <done>The repository has a working lint command that can run locally and in GitHub Actions.</done>
</task>

<task type="auto">
  <name>Create a named GitHub Actions workflow for push and pull-request quality gates</name>
  <files>.github/workflows/ci-pages.yml, package.json</files>
  <action>Author one workflow that runs on all pushes, pull requests, and manual dispatch, with separate `lint`, `test`, and `build` jobs using `npm ci` and cached Node setup. Keep job names short and stable because branch protection should be able to require them exactly as shown in pull requests.</action>
  <verify><automated>npm run lint && npm run test -- --run && npm run build</automated><manual>Inspect the workflow file to confirm the job names are exactly `lint`, `test`, and `build`.</manual><sampling_rate>Run after the workflow lands so the checked commands and the workflow's job wiring stay aligned.</sampling_rate></verify>
  <done>The repo has explicit CI quality gates ready for GitHub Actions and branch protection.</done>
</task>

<task type="auto">
  <name>Harden the workflow for readable failure visibility and merge gating</name>
  <files>.github/workflows/ci-pages.yml</files>
  <action>Add the remaining CI polish that keeps `main` green: stable permissions, sensible concurrency or cancellation behavior, and concise step naming so failures are easy to spot in GitHub Actions and pull requests. Do not collapse the workflow back into one opaque combined job.</action>
  <verify><automated>npm run lint</automated><manual>Review the workflow YAML to confirm failed jobs remain individually visible instead of hidden behind a single status.</manual><sampling_rate>Run after the workflow polish is applied because this is the plan-complete gate for CI-01 and CI-02 before deployment logic is added.</sampling_rate></verify>
  <done>The CI workflow is clear enough to serve as the repository's required merge gate.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- The repo exposes stable `lint`, `test`, and `build` commands.
- GitHub Actions runs those checks as separate named jobs on pushes and pull requests.
- The resulting job names are suitable for branch protection requirements on `main`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-and-github-pages-deployment/08-01-SUMMARY.md`
</output>
