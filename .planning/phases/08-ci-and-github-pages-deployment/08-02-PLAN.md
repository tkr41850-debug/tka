---
phase: 08-ci-and-github-pages-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .github/workflows/ci-pages.yml
  - vite.config.ts
  - src/App.tsx
  - src/App.test.tsx
  - src/lib/buildVersion.ts
  - src/styles.css
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
must_haves:
  truths:
    - Successful `main` runs deploy GitHub Pages from the uploaded build artifact instead of rebuilding different bits later.
    - The Vite build resolves the correct Pages base path so deployed assets load under the repository URL.
    - The live app shows subtle version metadata that helps confirm what release is currently published.
  artifacts:
    - { path: .github/workflows/ci-pages.yml, provides: "Gated Pages artifact upload and deploy job for main plus manual dispatch", contains: "deploy-pages" }
    - { path: vite.config.ts, provides: "Pages-aware Vite base handling for GitHub Actions builds", contains: "base" }
    - { path: src/lib/buildVersion.ts, provides: "Build metadata helper for package version and commit identifiers", contains: "buildVersion" }
    - { path: src/App.tsx, provides: "Subtle UI surfacing for the shipped build version", contains: "buildVersion" }
    - { path: src/App.test.tsx, provides: "Coverage for rendered build metadata or release label behavior", contains: "version" }
  key_links:
    - { from: .github/workflows/ci-pages.yml, to: vite.config.ts, via: "main-branch build artifact must already contain the correct Pages asset paths", pattern: "actions/upload-pages-artifact" }
    - { from: src/App.tsx, to: src/lib/buildVersion.ts, via: "release badge reads generated build metadata", pattern: "buildVersion" }
---

<objective>
Turn the checked build into a real GitHub Pages release path.

Purpose: Make the Vite output safe for repository-scoped Pages hosting, deploy the exact build artifact produced by CI, and surface low-emphasis version information in the app so production releases are visible without distracting from the writing workflow.
Output: Pages-aware build config, build-version helper, UI version label, and a gated deploy job for successful `main` or manual runs.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ci-and-github-pages-deployment/08-CONTEXT.md
@.planning/phases/08-ci-and-github-pages-deployment/08-RESEARCH.md
@.planning/phases/08-ci-and-github-pages-deployment/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Make the Vite production build safe for GitHub Pages paths</name>
  <files>vite.config.ts</files>
  <action>Update the Vite config so local development keeps the current root-path behavior while GitHub Actions builds can emit asset paths that work under the repository Pages URL. Keep the logic small, explainable, and resilient to repository renames or forks where practical.</action>
  <verify><automated>npm run build</automated><manual>Optional local `npm run preview` smoke check against the built app if the base-path logic feels risky.</manual><sampling_rate>Run immediately after the Vite update because broken asset paths are the highest-risk deployment failure in this phase.</sampling_rate></verify>
  <done>The production build emits Pages-safe asset URLs without harming local development.</done>
</task>

<task type="auto">
  <name>Add subtle build-version metadata to the shipped app surface</name>
  <files>src/lib/buildVersion.ts, src/App.tsx, src/App.test.tsx, src/styles.css</files>
  <action>Create a small helper that derives package-version and commit-aware build metadata, render it in a low-emphasis location within the app shell, and cover the visible behavior with tests. Keep the presentation understated so release visibility improves without turning the interface into an ops console.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional visual review to confirm the version label remains subtle on desktop and mobile layouts.</manual><sampling_rate>Run after the release label lands because DEPLOY-02 depends on this UI behavior being real and not only documented.</sampling_rate></verify>
  <done>The live site exposes a lightweight release identifier that helps confirm what build is deployed.</done>
</task>

<task type="auto">
  <name>Deploy the uploaded Pages artifact only after successful main-branch quality gates</name>
  <files>.github/workflows/ci-pages.yml</files>
  <action>Extend the workflow so the `build` job prepares the Pages artifact and a separate `deploy` job runs only for successful `main` or manual dispatch executions, using the official Pages actions and environment wiring. Preserve clear failed deployment visibility and manual retry support instead of hiding deploy errors behind retries or rebuilds.</action>
  <verify><automated>npm run lint && npm run test -- --run src/App.test.tsx && npm run build</automated><manual>Review the workflow to confirm `deploy` depends on `build`, uses the Pages artifact flow, and is restricted to `main` plus manual dispatch.</manual><sampling_rate>Run after deployment wiring lands because this closes the core DEPLOY-01 path before repo docs are written.</sampling_rate></verify>
  <done>The repository can publish the exact successful build artifact to one production GitHub Pages site.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
</verification>

<success_criteria>
- Vite builds work correctly for GitHub Pages repository paths.
- The app displays subtle build-version metadata.
- Successful `main` runs deploy the uploaded Pages artifact through a separate gated deploy job.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-and-github-pages-deployment/08-02-SUMMARY.md`
</output>
