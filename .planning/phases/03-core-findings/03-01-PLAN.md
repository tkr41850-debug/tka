---
phase: 03-core-findings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/analysis/types.ts
  - src/features/analysis/lib/parseDraft.ts
  - src/features/analysis/lib/findings/detectLengthAndWordiness.ts
  - src/features/analysis/lib/analyzeDraft.ts
  - src/features/analysis/lib/analyzeDraft.test.ts
autonomous: true
requirements:
  - CLAR-01
  - CLAR-02
  - CLAR-06
must_haves:
  truths:
    - User can receive deterministic findings for overly long sentences, overly long paragraphs, and supported filler phrases.
    - User can trust each deterministic finding to include a stable text location and matched excerpt.
    - The analyzer returns findings in one predictable order before any UI rendering begins.
  artifacts:
    - { path: src/features/analysis/types.ts, provides: "DraftAnalysis, finding metadata, severity, and location contracts", contains: "export type DraftFinding" }
    - { path: src/features/analysis/lib/parseDraft.ts, provides: "Shared sentence and paragraph parsing with text spans and counts", contains: "export function parseDraft" }
    - { path: src/features/analysis/lib/findings/detectLengthAndWordiness.ts, provides: "Deterministic sentence, paragraph, and filler detectors", contains: "export function detectLengthAndWordiness" }
    - { path: src/features/analysis/lib/analyzeDraft.ts, provides: "Aggregate analyzer that parses once and returns prioritized findings", contains: "export function analyzeDraft" }
    - { path: src/features/analysis/lib/analyzeDraft.test.ts, provides: "Fixture coverage for deterministic rule output and ordering", contains: "describe('analyzeDraft'" }
  key_links:
    - { from: src/features/analysis/lib/analyzeDraft.ts, to: src/features/analysis/lib/parseDraft.ts, via: "shared parse-once pipeline", pattern: "parseDraft" }
    - { from: src/features/analysis/lib/analyzeDraft.ts, to: src/features/analysis/lib/findings/detectLengthAndWordiness.ts, via: "deterministic detector aggregation", pattern: "detectLengthAndWordiness" }
    - { from: src/features/analysis/lib/analyzeDraft.test.ts, to: src/features/analysis/lib/analyzeDraft.ts, via: "fixture-driven rule verification", pattern: "analyzeDraft" }
---

<objective>
Build the reusable analysis core for Phase 3's deterministic findings before heuristic warnings or UI rendering land.

Purpose: Give the product one typed draft-analysis contract and one parse-once rule engine so later passive-voice, tense, and UI work can extend a stable foundation instead of duplicating parsing logic.
Output: Shared finding types, span-aware parsing helpers, deterministic detectors for long sentences/paragraphs and filler phrases, plus unit coverage for aggregate ordering.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-findings/03-RESEARCH.md
@.planning/phases/02-responsive-analysis-loop/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Define the Phase 3 draft-analysis and finding contracts</name>
  <files>src/features/analysis/types.ts</files>
  <action>Extend the analysis type layer so the worker can return a richer `DraftAnalysis` object instead of only a metrics snapshot. Add explicit finding contracts for rule id, rule label, severity, explanation, matched text, and start/end offsets plus human-readable sentence or paragraph location labels. Keep the contracts serializable and compatible with the existing Phase 2 worker protocol, and do not add user-configurable rule settings yet.</action>
  <verify><automated>npm run build</automated><manual>None.</manual><sampling_rate>Run immediately after the type update so all later files compile against the same analysis contract.</sampling_rate></verify>
  <done>The repo has one typed vocabulary for deterministic findings and aggregate draft analysis, and the build still passes after the new contracts land.</done>
</task>

<task type="auto">
  <name>Create the shared parser and deterministic length-plus-wordiness detectors</name>
  <files>src/features/analysis/lib/parseDraft.ts, src/features/analysis/lib/findings/detectLengthAndWordiness.ts, src/features/analysis/lib/analyzeDraft.ts</files>
  <action>Create a parse helper that normalizes English-only sentence and paragraph spans with word counts, paragraph sentence counts, and matched excerpts derived from the raw draft. On top of that helper, implement deterministic findings for sentences over the default 28-word limit, paragraphs over the default 6-sentence limit, and a small curated filler-phrase pack such as `in order to`, `please note`, `simply`, `just`, `at this point in time`, and `due to the fact that`. Return aggregate findings already sorted by severity, then rule priority, then earliest offset, and keep the implementation dependency-free.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts</automated><manual>None.</manual><sampling_rate>Run after the parser and deterministic detectors are wired so the aggregate analyzer is proven before heuristic rules build on it.</sampling_rate></verify>
  <done>The repo can parse one draft once, emit deterministic clarity findings with stable spans, and produce a consistently ordered aggregate result.</done>
</task>

<task type="auto">
  <name>Add fixture coverage for deterministic findings and ordering</name>
  <files>src/features/analysis/lib/analyzeDraft.test.ts</files>
  <action>Write focused unit fixtures that prove long-sentence, long-paragraph, and filler findings fire at the intended defaults, include meaningful excerpts and locations, and sort in a stable order when multiple rules match. Include at least one no-findings fixture so the analyzer's clean path is explicit. Finish by running the test file plus a build to confirm the new analysis contracts remain type-safe.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run after the fixtures land because later heuristic detectors will depend on this aggregate analyzer staying stable.</sampling_rate></verify>
  <done>The project has automated proof that deterministic Phase 3 findings fire with stable metadata and predictable ordering.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/features/analysis/lib/analyzeDraft.test.ts`
- `npm run build`
</verification>

<success_criteria>
- One typed `DraftAnalysis` contract exists for aggregate findings and location metadata.
- Long sentences, long paragraphs, and supported filler phrases are detected through one shared parse pipeline.
- Deterministic rule ordering and location output are covered before heuristic warnings are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-findings/03-01-SUMMARY.md`
</output>
