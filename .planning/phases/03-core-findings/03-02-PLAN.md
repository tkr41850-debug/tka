---
phase: 03-core-findings
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/features/analysis/types.ts
  - src/features/analysis/lib/findings/detectVoiceAndTense.ts
  - src/features/analysis/lib/analyzeDraft.ts
  - src/features/analysis/lib/findings/detectVoiceAndTense.test.ts
autonomous: true
requirements:
  - CLAR-03
  - CLAR-04
must_haves:
  truths:
    - User can see likely passive-voice warnings called out separately from deterministic clarity issues.
    - User can see likely tense-drift warnings when sentences depart from the default present-tense guidance.
    - Heuristic findings still arrive in one stable prioritized list without hiding their lower confidence.
  artifacts:
    - { path: src/features/analysis/lib/findings/detectVoiceAndTense.ts, provides: "Conservative passive-voice and tense-drift heuristics with explanation copy", contains: "export function detectVoiceAndTense" }
    - { path: src/features/analysis/lib/analyzeDraft.ts, provides: "Aggregate analyzer that merges heuristic and deterministic findings into one ranked list", contains: "detectVoiceAndTense" }
    - { path: src/features/analysis/lib/findings/detectVoiceAndTense.test.ts, provides: "Fixture coverage for passive-voice and tense-drift heuristics", contains: "describe('detectVoiceAndTense'" }
    - { path: src/features/analysis/types.ts, provides: "Rule metadata that distinguishes likely heuristic findings from deterministic ones", contains: "confidence" }
  key_links:
    - { from: src/features/analysis/lib/findings/detectVoiceAndTense.ts, to: src/features/analysis/lib/parseDraft.ts, via: "sentence-level heuristic evaluation against shared spans", pattern: "sentences" }
    - { from: src/features/analysis/lib/analyzeDraft.ts, to: src/features/analysis/lib/findings/detectVoiceAndTense.ts, via: "heuristic detector aggregation and ranking", pattern: "detectVoiceAndTense" }
    - { from: src/features/analysis/lib/findings/detectVoiceAndTense.test.ts, to: src/features/analysis/lib/findings/detectVoiceAndTense.ts, via: "fixture-based false-positive and positive checks", pattern: "detectVoiceAndTense" }
---

<objective>
Add trustworthy heuristic language warnings on top of the deterministic Phase 3 analysis core.

Purpose: Deliver the first browser-local passive-voice and tense-drift review pass while keeping trust high through conservative matching, explicit `likely` language, and targeted fixture coverage.
Output: Voice-and-tense detector module, richer finding metadata for heuristic confidence, aggregate analyzer updates, and unit tests that protect against noisy matches.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-findings/03-RESEARCH.md
@.planning/phases/03-core-findings/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement conservative passive-voice and tense-drift heuristics</name>
  <files>src/features/analysis/lib/findings/detectVoiceAndTense.ts, src/features/analysis/types.ts</files>
  <action>Create a detector that evaluates the shared parsed sentences from plan 01 and emits `likely passive voice` findings when common be-verbs are followed by likely participles, with stronger explanation copy when a `by` phrase appears nearby. In the same module, add present-tense-guidance heuristics that flag obvious past or future markers such as `was`, `were`, `had`, and `will` only when they meaningfully depart from the default guidance. Extend the shared finding metadata as needed for heuristic confidence and explanations, but keep the implementation small, local, and explicit about uncertainty.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/findings/detectVoiceAndTense.test.ts</automated><manual>None.</manual><sampling_rate>Run as soon as the heuristic module lands because these rules are the noisiest and need immediate fixture feedback.</sampling_rate></verify>
  <done>The repo has one conservative heuristic detector for passive voice and tense drift, and the finding contracts can express lower-confidence guidance clearly.</done>
</task>

<task type="auto">
  <name>Merge heuristic findings into the aggregate analyzer and ranking model</name>
  <files>src/features/analysis/lib/analyzeDraft.ts</files>
  <action>Update the aggregate analysis entry to combine the new heuristic findings with the deterministic findings from plan 01 while preserving a stable priority order. Deterministic clarity findings should remain higher priority than likely passive or tense warnings when severity is otherwise equal, and clean drafts should still return an empty findings list without special-case UI data massaging. Keep ranking in the analysis layer, not React.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/findings/detectVoiceAndTense.test.ts src/features/analysis/lib/analyzeDraft.test.ts</automated><manual>None.</manual><sampling_rate>Run after ranking logic changes so the combined analyzer stays deterministic before UI work consumes it.</sampling_rate></verify>
  <done>The aggregate analyzer returns one merged, stable findings list that includes deterministic and heuristic Phase 3 rules.</done>
</task>

<task type="auto">
  <name>Add heuristic fixtures that cover positive matches and obvious false positives</name>
  <files>src/features/analysis/lib/findings/detectVoiceAndTense.test.ts, src/features/analysis/lib/analyzeDraft.test.ts</files>
  <action>Write fixtures for passive constructions, simple present-tense technical prose, and clear tense departures so the rule pack proves both what should fire and what should stay quiet. Include at least one passive false-positive guard such as adjectival `is ready`-style wording, and at least one future-tense exception where explicit future wording is intentional but still should be surfaced as a likely drift warning. Finish by rerunning both targeted test files and a build.</action>
  <verify><automated>npm run test -- --run src/features/analysis/lib/findings/detectVoiceAndTense.test.ts src/features/analysis/lib/analyzeDraft.test.ts && npm run build</automated><manual>None.</manual><sampling_rate>Run after the fixtures land because this is the quality gate for the heuristic rule pack before UI exposure.</sampling_rate></verify>
  <done>The project has automated proof that heuristic findings are conservative, stable, and integrated into the aggregate analyzer.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/features/analysis/lib/findings/detectVoiceAndTense.test.ts src/features/analysis/lib/analyzeDraft.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Likely passive voice and tense drift are detected conservatively and labeled as heuristic findings.
- The aggregate analyzer merges heuristic and deterministic findings into one stable priority order.
- Fixture coverage protects against at least one common passive false positive and one obvious tense-drift case.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-findings/03-02-SUMMARY.md`
</output>
