---
phase: 03-core-findings
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/features/workspace/types.ts
  - src/features/analysis/workers/localAnalysis.worker.ts
  - src/App.tsx
  - src/App.test.tsx
  - src/features/workspace/components/WorkspaceSnapshot.tsx
  - src/styles.css
autonomous: true
requirements:
  - REVW-02
  - CLAR-01
  - CLAR-02
  - CLAR-03
  - CLAR-04
  - CLAR-06
must_haves:
  truths:
    - User can review one prioritized findings list that shows severity, rule name, and where each issue appears in the draft.
    - User can keep trusting the stale-versus-current messaging from Phase 2 while the visible findings update in the background.
    - User still sees the local snapshot metrics alongside the new findings list, so the current analysis surface becomes richer without becoming a different workflow.
  artifacts:
    - { path: src/features/analysis/workers/localAnalysis.worker.ts, provides: "Worker response updated to compute aggregate draft analysis with findings", contains: "analyzeDraft" }
    - { path: src/App.tsx, provides: "App orchestration that stores the richer draft analysis result", contains: "setSnapshot" }
    - { path: src/features/workspace/components/WorkspaceSnapshot.tsx, provides: "Prioritized findings list with severity, rule label, and location copy", contains: "findings" }
    - { path: src/App.test.tsx, provides: "Integration coverage for rendered findings and freshness behavior", contains: "background analysis" }
    - { path: src/styles.css, provides: "Visual treatment for findings severity and list layout", contains: ".finding" }
  key_links:
    - { from: src/features/analysis/workers/localAnalysis.worker.ts, to: src/features/analysis/lib/analyzeDraft.ts, via: "worker-side aggregate analysis execution", pattern: "analyzeDraft" }
    - { from: src/App.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "draft analysis prop handoff", pattern: "WorkspaceSnapshot" }
    - { from: src/App.test.tsx, to: src/features/workspace/components/WorkspaceSnapshot.tsx, via: "user-visible findings assertions", pattern: "rule name" }
---

<objective>
Turn the new Phase 3 rule pack into a visible review surface inside the shipped app.

Purpose: Fulfill the phase goal by carrying the worker-generated findings into the existing analysis panel, preserving the trusted Phase 2 freshness model while exposing a prioritized review list users can act on later.
Output: Worker and app wiring for the richer analysis shape, findings-aware snapshot UI, severity styling, and integration coverage for rendered issue review.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-findings/03-RESEARCH.md
@.planning/phases/03-core-findings/03-02-SUMMARY.md
@.planning/phases/02-responsive-analysis-loop/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Wire the richer draft-analysis result through the worker and app shell</name>
  <files>src/features/workspace/types.ts, src/features/analysis/workers/localAnalysis.worker.ts, src/App.tsx</files>
  <action>Update the worker path so it computes the aggregate Phase 3 analysis result instead of only the earlier snapshot helper output, while still preserving all Phase 2 request-id and freshness behavior. Replace any app-level assumptions that the accepted result is only metrics data, but keep one authoritative draft state, the existing queued/running/fresh lifecycle flow, and the stale-result protection intact. Do not add navigation, inline highlights, or rewrite actions yet.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>None.</manual><sampling_rate>Run right after the worker/app wiring lands so the richer result shape is proven before UI rendering details are added.</sampling_rate></verify>
  <done>The app stores the richer draft-analysis result from the worker without regressing the existing background-refresh behavior.</done>
</task>

<task type="auto">
  <name>Render the prioritized findings list inside the analysis panel</name>
  <files>src/features/workspace/components/WorkspaceSnapshot.tsx, src/styles.css</files>
  <action>Expand the current snapshot panel so it still shows the metrics summary, then adds a findings section with a readable severity marker, rule label, location text, explanation copy, and matched excerpt for each item in priority order. Preserve the existing paper-and-ink visual language instead of introducing a generic issue dashboard, and make the empty state explicit when the current draft has no findings. Keep this phase list-only: no inline highlight navigation, no dismiss buttons, and no rewrite controls.</action>
  <verify><automated>npm run test -- --run src/App.test.tsx</automated><manual>Optional local review to confirm the findings section reads clearly on both narrow and wide layouts.</manual><sampling_rate>Run after the rendering update so visible ordering and empty-state copy stay aligned with the worker output.</sampling_rate></verify>
  <done>The shipped analysis panel shows prioritized findings with severity, rule, and location while preserving existing metrics and freshness messaging.</done>
</task>

<task type="auto">
  <name>Add integration coverage for findings rendering, ordering, and freshness recovery</name>
  <files>src/App.test.tsx</files>
  <action>Expand the app workflow tests so mocked worker responses can return multiple Phase 3 findings and the UI proves their rendered order, severity text, rule labels, and location copy. Keep the existing queued/running/error freshness assertions, and add at least one clean-draft case where the UI states that no core findings were detected. Finish by running the full test suite and build as the phase-complete gate.</action>
  <verify><automated>npm run test -- --run && npm run build</automated><manual>None.</manual><sampling_rate>Run after all Phase 3 UI behavior is wired because this is the phase-complete integration gate.</sampling_rate></verify>
  <done>The project has end-to-end proof that prioritized findings appear in the UI without breaking Phase 2 background-analysis trust behavior.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run`
- `npm run build`
</verification>

<success_criteria>
- The worker returns a richer draft-analysis result without breaking the Phase 2 refresh lifecycle.
- The analysis panel renders prioritized findings with severity, rule label, location, explanation, and empty-state copy.
- Integration coverage proves findings rendering, ordering, clean-draft behavior, and freshness recovery end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-findings/03-03-SUMMARY.md`
</output>
